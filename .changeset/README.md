# Changesets

This folder holds [changesets](https://github.com/changesets/changesets) used to version and
publish Humb's public packages.

When you make a user-facing change to a published package, run `pnpm changeset` and follow the
prompts to describe the change and select a semver bump.
