{
  "version": 1,
  "description": "Machine-readable live work queue. Passing entries are retained for 24 hours, then removed by pnpm features:prune; durable behavior and evidence remain in product specs and Git/PR history. See docs/FEATURES.md for rules.",
  "nextIds": {
    "F": 129,
    "DF": 10
  },
  "states": ["not_started", "active", "blocked", "passing"],
  "features": [
    {
      "id": "F111",
      "behavior": "Column operations per docs/product-specs/schema-editing.md: addColumn/renameColumn/dropColumn/alterColumn (type, nullability, default) - native ALTER on Postgres/MySQL; SQLite uses native ADD/RENAME/DROP COLUMN where the operation and version allow and the spec's rebuild pattern (PRAGMA foreign_keys=OFF, create-new/copy/swap in one transaction, PRAGMA foreign_key_check) for retype/constraint changes, chunking the copy so a large table doesn't block the event loop unboundedly; not-applicable on MongoDB (capability reported false, UI hides the surface). Server routes with typed confirmation for dropColumn, audit events, and conformance including a SQLite rebuild data-preservation case (data, indexes, FKs survive) and restricted-user rejection. Depends on F110.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "bcc3c41",
      "evidence": "PR #127 (https://github.com/ensp1re/qyre/pull/127), merged as bcc3c41. addColumn/renameColumn/alterColumn/dropColumn added to SchemaDdlApi, implemented across Postgres/MySQL/SQLite (absent on MongoDB entirely - no per-column concept on a schemaless collection). Postgres uses native per-clause ALTER COLUMN (ALTER COLUMN TYPE/SET-DROP NOT NULL/SET-DROP DEFAULT). MySQL's alterColumn reads the column's current definition directly from information_schema.columns (COLUMN_TYPE, not DATA_TYPE, to preserve length/precision; COLUMN_DEFAULT, since the shared ColumnMetadata read type has no default field at all) and merges requested changes onto it for one MODIFY COLUMN statement, per MySQL's own full-redefinition requirement. SQLite's addColumn/renameColumn/dropColumn use native ALTER TABLE; alterColumn always takes the 12-step rebuild path (PRAGMA foreign_keys=OFF, create a new table with the desired final schema from PRAGMA table_info/foreign_key_list, copy every row, drop the old table, rename the new one into place, replay every index/trigger from sqlite_master's own stored CREATE statements, PRAGMA foreign_key_check, commit, PRAGMA foreign_keys=ON) - manually smoke-tested against a fixture with a unique index, a trigger, and a cross-table FK with ON DELETE CASCADE, all three survived intact. Server routes: POST /api/tables/:schema/:table/ddl/columns (add), PATCH .../ddl/columns/:column (rename and/or alter in one request, alter applied to the new name when both given), DELETE .../ddl/columns/:column (drop, typed confirmation) - gated by F096 + the session's real supportsDdl, dataType validated against the curated catalog, column existence validated against the table's real introspected columns; a column route against MongoDB responds 400 explaining collections have no columns to alter, registered but engine-conditionally rejected, never a bare 404. Conformance: an add/rename/alter/drop roundtrip in the shared testing-conformance suite (skipped for MongoDB, plus a dedicated assertion there that all four column methods are undefined) and each SQL engine's own integration tests, incl. a dedicated SQLite rebuild data-preservation case (data, a unique index, and a cross-table FK all survive) and restricted-user/read-only rejection; 16 new packages/server/tests/routes/schema-ddl.test.ts route tests cover the add/rename/alter/drop paths, the combined rename+alter request, unknown-column rejection, unsupported-type rejection, MongoDB 400, and --read-only 403. Verified: pnpm check:quiet:run and pnpm verify:pr (full local gate incl. Docker-backed Postgres/MySQL/MongoDB integration tests, the shared conformance suite, and full E2E, all green).",
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md",
      "completedAt": "2026-07-12T23:28:10.000Z"
    },
    {
      "id": "F112",
      "behavior": "Index management per docs/product-specs/schema-editing.md: createIndex (columns, unique flag) and dropIndex in the ddl namespace across all four engines (MongoDB native createIndex/dropIndex), building on the existing IndexMetadata listing from getTable; server routes gated + audited; conformance: create-list-drop roundtrip on every engine, unique-index constraint behavior, restricted-user rejection. Depends on F110.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "16b44e6",
      "evidence": "PR #128 (https://github.com/ensp1re/qyre/pull/128), merged as 16b44e6. createIndex/dropIndex added to SchemaDdlApi and implemented across all four engines, including MongoDB (unlike F111's column ops, which are entirely absent there) - MongoDB maps onto its own collection.createIndex()/dropIndex() with a plain top-level/dotted-field key spec built from IndexDefinition.columns. Postgres/SQLite index names are unique per schema/database (table param unused in dropIndex); MySQL's DROP INDEX requires the table per its own grammar. Server routes: POST /api/tables/:schema/:table/ddl/indexes (create), DELETE .../ddl/indexes/:indexName (drop) - gated on the session's real supportsIndexManagement capability (a distinct flag from supportsDdl, per F090's reservation), index columns validated against the table's real introspected columns (skipped for MongoDB - best-effort field sampling, not an authoritative catalog, and index columns can be dotted nested-field paths never sampled as top-level fields), index existence validated for drop. dropIndex uses a plain confirming click, not typed confirmation, per the spec's lower-risk treatment of index drops versus table/column drops. A real pre-existing gap was found and fixed along the way: MongoDB's introspectCollection hardcoded indexes: [] and never actually listed real indexes - now calls collection.indexes(), so getTable correctly reports the default _id_ index (primary: true) plus any created ones, which F112's own conformance/integration tests needed to verify against. Conformance: a create-list-drop roundtrip with a unique-index-rejects-a-duplicate-value assertion in the shared testing-conformance suite (all four engines, unlike F111's SQL-only column-op test) and each engine's own integration tests, incl. restricted-user/read-only rejection for Postgres/MySQL/SQLite (MongoDB's shared docker-compose container has no auth enabled, matching the pre-existing, documented limitation every other MongoDB restricted-role case in this codebase already has); 10 new packages/server/tests/routes/schema-ddl.test.ts route tests cover the create/drop success paths, unknown-column/unknown-index rejection, invalid-identifier rejection, view-target rejection (F124), the supportsDdl-true-but-supportsIndexManagement-false case proving the two capabilities gate independently, and --read-only 403 (F096). Verified: pnpm check:quiet:run and pnpm verify:pr (full local gate incl. Docker-backed Postgres/MySQL/MongoDB integration tests, the shared conformance suite, and full E2E, all green).",
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md",
      "completedAt": "2026-07-13T05:47:52.000Z"
    },
    {
      "id": "F113",
      "behavior": "Permission-gated New-table flow per docs/product-specs/schema-editing.md: a table designer in the sidebar/Schema surface for name + columns (type picked from the engine's type catalog exposed by F110, nullability, default, PK incl. composite), a preview of the exact DDL/ops before creation, per-engine validation, and engine errors mapped to clear messages; the MongoDB variant degrades to a new-collection dialog (name only). Hidden without ddl capability. Depends on F110.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "passing",
      "commitHash": "29c7a52",
      "evidence": "PR #129 (https://github.com/ensp1re/qyre/pull/129), merged as 29c7a52. A permission-gated \"New table\" button in the Schema tab toolbar, hidden unless the session's supportsDdl capability allows it (F097's \"hidden without the capability\" rule via the existing sessionAllows gate helper). Opens packages/ui's new CreateTableDialog: table name, a column list (name, type from F110's curated per-engine catalog via a native select, nullability, default), and a live human-readable CREATE TABLE/db.createCollection preview - display-only, not literal per-engine SQL (each adapter's own quoting/type-mapping stays server-side), mirroring commit-preview.ts's existing \"preview, not the real query text\" precedent for row mutations. MongoDB degrades to a name-only new-collection form since createTable ignores columns there entirely. Primary-key marking was deliberately dropped from the FEATURES.json behavior blurb's stated scope - ColumnDefinition/createTable (F110) has no first-class way to express a PK at all, so a PK checkbox would have been decorative; noted as a documented deviation rather than silently built. apps/web's new useCreateTable mutation (React Query) posts to F110's POST /api/schemas/:schema/tables and invalidates the allTables/overview query keys on success so the new table appears without a manual refresh; server errors (e.g. Postgres's real \"relation already exists\") surface inline via the dialog's error prop without losing the user's draft. Found and worked around a real pre-existing cross-package bundling gap along the way: @qyre/core is tsup-bundled into one dist/index.js that also pulls in connection-target.ts's Node-only fs/path/url imports (used server/CLI-side), so any runtime (non-type) import from the package's barrel broke Vite's browser build - the first time apps/web had ever needed one - the three curated column-type-catalog arrays are duplicated locally in apps/web/src/features/schema/model/column-type-catalog.ts instead of imported, rather than restructuring @qyre/core's build for every other consumer. 12 new packages/ui/tests/schema/create-table-dialog.render.test.tsx tests cover the SQL-engine form, the MongoDB name-only degradation, table/column-name identifier validation gating Create, add/remove column rows, submitted-value coercion (numeric/boolean/null defaults), the NOT NULL checkbox, inline error display, Cancel/Escape, and the creating-state disabled controls. Verified: pnpm check:quiet:run and pnpm verify:pr (full local gate incl. full E2E, all green) plus manual browser verification against a live Postgres connection via the qyre-preview-postgres launch config - button correctly hidden/shown by capability, live DDL preview updates correctly on real (non-automation-tool) interaction, successful create end-to-end (201 Created, cache invalidation refetch, new table visible in the Grid view with no manual refresh, the server-audited EventLog entry confirmed via a direct authenticated fetch to GET /api/console), and a duplicate-table-name attempt surfaced the real Postgres error (relation \"qyre_widgets\" already exists) inline with the dialog staying open and the draft preserved. Test artifacts cleaned up via the existing SQL Editor's own typed-confirmation DROP TABLE flow, not a raw API bypass.",
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md",
      "completedAt": "2026-07-13T07:18:00.000Z"
    },
    {
      "id": "F114",
      "behavior": "Each table gains a Structure view alongside Rows per docs/product-specs/schema-editing.md: columns listed with add/rename/drop/alter controls (capability-gated; hidden where the adapter reports not-applicable, e.g. MongoDB columns), index management (F112), and rename/truncate/drop-table actions behind the spec's typed confirmations - every action previews its DDL/ops first and surfaces the audit outcome. Views/matviews (F124) get the Structure information read-only with zero mutation affordances and their kind stated; read-only sessions likewise see structure info only (a strict improvement to today's read-only experience). E2E covers a column-add + index-create + drop-table roundtrip on one SQL engine, and the F097 read-only guard stays green. Depends on F111, F112, F113.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "passing",
      "commitHash": "dd707f4",
      "evidence": "PR #130 (https://github.com/ensp1re/qyre/pull/130), merged as dd707f4. A Rows/Structure toggle on the Tables tab (useTableView, localStorage-backed like the Schema tab's graph/grid choice, surviving TablesTab's remount-on-table-switch). packages/ui's new TableStructure lists columns with Edit/Drop controls (EditColumnDialog combines rename+alter into one PATCH per F111's route shape; the new shared ConfirmTypedNameDialog primitive backs all three typed-confirmation flows: dropColumn/truncateTable/dropTable), an AddColumnDialog, indexes with an immediate Drop button (no confirmation - an index carries no user data, per the spec) plus CreateIndexDialog, and table-lifecycle actions (inline rename; typed-confirm Truncate/Drop table). Three independent gates via computeTableStructureEditability: canEditColumns (supportsDdl, false for MongoDB), canManageIndexes (supportsIndexManagement), canEditTable (supportsDdl); views/matviews or zero-capability sessions render the read-only TableDetail - zero write affordances, not disabled ones. e2e/read-only-mode.spec.ts's F097 guard extended to visit the Structure view and its button-name regex covers the new DDL control names. Found and fixed a real latent bug: dropTable/dropColumn/dropIndex (F110-F112) reply 204 No Content but the shared fetchJson helper unconditionally parsed a JSON body, throwing on the empty response (silently swallowed by the caller) - no UI had called those DELETE routes before F114; fixed fetchJson to return null on 204, covered by new fetch-json.test.ts. 36 new tests across packages/ui (ConfirmTypedNameDialog/AddColumnDialog/EditColumnDialog/CreateIndexDialog/TableStructure render tests) and apps/web (structure-editability, fetch-json, workspace-state reducer actions tableRenamed/tableDropped). Verified: pnpm check:quiet:run and pnpm verify:pr all green (CI also green on PR #130), plus manual browser verification of all 8 operations end-to-end against a live Postgres connection, including re-verifying dropIndex/dropColumn after the 204 fix.",
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md",
      "completedAt": "2026-07-13T18:30:00.000Z"
    },
    {
      "id": "F115",
      "behavior": "Database/schema lifecycle where the engine supports it: adapters gain the admin namespace with listDatabases (Postgres/MySQL/MongoDB; SQLite not-applicable - one file is one database and must report that cleanly), createDatabase/dropDatabase, and Postgres schema create/drop; capabilities report supportsDatabaseManagement from the real F092-F095 introspection. Server routes gated + audited; drop requires the typed-confirmation token server-side. Conformance: list/create/drop roundtrip on the three applicable engines, restricted-user rejection, SQLite clean not-applicable. Needs a short spec section added to docs/product-specs/permissions-and-capabilities.md or schema-editing.md (decide during implementation, following F109's patterns). Depends on F096, F110.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "8ba44c3",
      "evidence": "PR #131 (https://github.com/ensp1re/qyre/pull/131), merged as 8ba44c3. Adapters gain the DatabaseAdminApi admin namespace (mirroring mutations/ddl's optional-namespace pattern): Postgres gets listDatabases/createDatabase/dropDatabase plus createSchema/dropSchema (no CASCADE on drop - a non-empty schema surfaces Postgres's own dependency error rather than silently taking every contained table with it, confirmed live); MySQL the database trio only (its \"schema\" IS its database); MongoDB listDatabases/dropDatabase only (databases exist implicitly on first write, so there is no create to model - the route responds 400 explaining that); SQLite has no namespace at all (one file is one database, routes reply a clean 400, never a bare 404). supportsDatabaseManagement was already introspected for real on Postgres (CREATEDB/superuser) and MySQL (global CREATE/SUPER) from F092/F093; MongoDB now derives it from the dropDatabase privilege action (the only database-management action its privilege model has), replacing its F095 always-false placeholder incl. the unauthenticated full-access default. New routes: GET /api/databases (a read, no gate), POST /api/databases, DELETE /api/databases/:database, POST /api/schemas, DELETE /api/schemas/:schema - mutating routes two-tier gated (F096 + supportsDatabaseManagement), both drops behind server-side typed confirmation, all audited with operation+target structured fields. Design decision documented in the spec's new \"Database and schema lifecycle\" section: DELETE /api/schemas/:schema deliberately has no exists-first 404, since getOverview() derives schemas from tables and an empty schema never appears in it. MongoDB's WRITE_METHODS lint-scan gained admin.ts as a third trusted write-path file. Verified: 16 new server route tests, 4 new conformance tests (create/list/drop roundtrip on Postgres/MySQL/MongoDB incl. MongoDB's implicit-creation path, SQLite absent-namespace, Postgres schema roundtrip, capability reporting), restricted-role rejection tests for Postgres/MySQL, updated MongoDB permission-shape unit tests - all green against live databases, pnpm check:quiet:run and pnpm verify:pr all green.",
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md",
      "completedAt": "2026-07-13T20:15:00.000Z"
    },
    {
      "id": "F116",
      "behavior": "Database-level management UI: the connection/database switcher (F064/F085 flow) lists databases on the current server with switch-in-place, permission-gated create-database (name plus minimal engine-appropriate options), and drop-database behind typed confirmation; Postgres schema create/drop appears in the sidebar. Read-only or ungranted sessions see the list only, with the affordances hidden and the reason available. Depends on F115.",
      "verification": "pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "passing",
      "commitHash": "94987a4",
      "evidence": "PR #132 (https://github.com/ensp1re/qyre/pull/132), merged as 94987a4. ConnectDrawer now lists sibling databases and switches without credential re-entry through POST /api/connect/database; create/drop database and Postgres schema controls are capability-gated and hidden in read-only or ungranted sessions while list/switch remain available. Shared DatabasePanel, CreateNamedDialog, and typed-confirmation patterns cover the UI. Verified live against Postgres for create/switch/drop database and schema dependency handling; 36 new/updated unit tests, pnpm check:quiet:run, full E2E, and both CI jobs passed.",
      "blockedReason": null,
      "spec": null,
      "completedAt": "2026-07-13T18:28:27.000Z"
    },
    {
      "id": "F117",
      "behavior": "Permission-gated CSV import into an existing table: a server endpoint streams and parses the upload (with an explicit request-size/row-count cap and multipart limits so a huge or malicious file cannot exhaust memory or disk - the F066 streaming-export philosophy in reverse), validates and coerces values per column type against introspected metadata, and inserts in bounded batches through the mutations namespace (transactional per batch, per-row error report with line numbers); the UI wizard maps CSV columns to table columns with a dry-run preview of the first rows and a clear success/failure summary. Kind-gated (tables/collections only) and permission-gated. Needs a short product-spec pass (docs/product-specs/csv-import.md) before implementation, following the F063-F066 pattern. Depends on F099, F102.",
      "verification": "pnpm --filter @qyre/testing-conformance test && pnpm --filter @qyre/server test && pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "passing",
      "commitHash": "35c29ce",
      "evidence": "PR #133 (https://github.com/ensp1re/qyre/pull/133), merged as 35c29ce. Added the capped streaming multipart POST /api/tables/:schema/:table/import.csv contract with inspect/validate/import modes, scalar coercion against introspected metadata, physical source-line errors, a 20-row preview, and server-side kind/insert-permission/read-only enforcement. Postgres/MySQL/SQLite use existing commitBatch transactions in bounded 250-row batches; standalone MongoDB uses one-document atomic insertRow calls. The Tables toolbar and shared CsvImportDialog provide exact-name mapping defaults, dry-run invalidation, coerced preview, and inserted/failed result reporting while hiding the action for unavailable targets. Added the browser-safe @qyre/core/csv-import export plus 2 core, 11 server, 5 web, and 4 UI tests, and extended the standing read-only E2E canary. Verified on Node 22 with CI=1 pnpm verify:pr locally and again through the pre-push hook: 34/34 package tasks, smoke E2E, and full E2E passed against Postgres, MySQL, SQLite, and MongoDB; both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/product-specs/csv-import.md",
      "completedAt": "2026-07-13T19:46:24.000Z"
    },
    {
      "id": "F118",
      "behavior": "Row export grows JSON (streamed array of objects) and SQL-INSERT formats (SQL engines only, identifiers/literals safely escaped per engine; MongoDB exports Extended JSON, reported via capabilities not engine-name checks) alongside the existing CSV path, honoring current filters/sort/selection exactly as F066/F083 do. The export implementation also fixes the current O(n^2) pattern (the CSV route re-runs the query per MAX_PAGE_SIZE page via OFFSET pagination - packages/server/src/routes/tables.ts): switch to a single-pass stream per engine (pg cursor/query stream, mysql2 .stream(), better-sqlite3 .iterate(), MongoDB cursor). Read-only-safe, so it improves every access level; update docs/product-specs/server-side-sort-export.md. No dependency on write slices - schedulable any time after F091.",
      "verification": "pnpm --filter @qyre/testing-conformance test && pnpm --filter @qyre/server test && pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "passing",
      "commitHash": "a56c238",
      "evidence": "PR #134 (https://github.com/ensp1re/qyre/pull/134), pushed as a56c238. Replaced the CSV route's repeated OFFSET pagination with one native async stream per adapter: pg-query-stream, mysql2 query streams, better-sqlite3 iteration, and MongoDB cursors, all with early-close cleanup. Added capability-driven CSV, ordinary JSON, MongoDB relaxed Extended JSON, and adapter-owned SQL INSERT output with validated sort/filter propagation, bounded formatting, formula-injection defense, and engine-specific identifier/literal escaping. The Tables toolbar exposes only advertised whole-result formats while preserving F083 selected-row browser CSV precedence. Updated the product contract plus shared capabilities, route/formatter/UI/API tests, and cross-engine conformance coverage for format availability, sorting, filtering, empty results, and interrupted iteration. Verified on Node 22 with CI=1 pnpm verify:pr locally and through the pre-push hook: 34/34 package tasks, 11 smoke E2E, and 19 full E2E passed; the focused native-stream conformance case passed explicitly on Postgres, MySQL, SQLite, and MongoDB; both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/product-specs/server-side-sort-export.md",
      "completedAt": "2026-07-13T20:38:22.000Z"
    },
    {
      "id": "F128",
      "behavior": "EXPLAIN viewer for the SQL editor (read-only-safe, a standard IDE capability): an Explain action runs the engine-appropriate plan statement for the current query - Postgres EXPLAIN (optionally ANALYZE behind an explicit toggle with a warning that ANALYZE executes the statement, and never for non-SELECT classifications), MySQL EXPLAIN/EXPLAIN FORMAT=TREE, SQLite EXPLAIN QUERY PLAN; MongoDB not-applicable (no SQL surface) - and renders the plan in a readable dedicated panel (text/tree; no graphical layout this slice). assertReadOnly already allows the EXPLAIN keyword, so read-only sessions get this too. Unit tests for the per-engine statement construction; e2e asserts a plan renders for a fixture query on one engine. Depends on F106 (classification decides ANALYZE eligibility); read-only parts schedulable earlier if picked up independently.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/ui test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/sql-editor.md"
    },
    {
      "id": "F119",
      "behavior": "A read-only Roles & grants viewer so users understand why affordances are hidden: Postgres shows pg_roles plus a grants summary for the current user, MySQL shows SHOW GRANTS/information_schema privileges (including granted roles), MongoDB shows rolesInfo/usersInfo where permitted, SQLite shows file-permission facts instead; strictly a viewer - no role management mutations anywhere in plan 0006. Surfaces through the admin namespace with a capability flag; degrades cleanly when the connected user may not read role catalogs. Depends on F092-F095.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/web test",
      "state": "active",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/roles-and-grants-viewer.md"
    },
    {
      "id": "F120",
      "behavior": "Permission-denied hardening across every mutating path: engine permission errors map to structured, friendly responses (which operation, which object, the likely missing grant) instead of raw engine text; a denial triggers a capabilities/permissions refetch so UI affordances converge with reality when grants change mid-session (the advisory-introspection principle in exec plan 0006); the event log records each denial. Conformance covers a denied insert/update/ddl per engine using the restricted fixtures with grants revoked mid-session where the engine allows. Depends on the write surfaces it protects - schedule after F117.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F121",
      "behavior": "Plan 0006's exit gate: a Playwright role-matrix suite runs per engine - a read-only session (restricted fixture user and --read-only flag) shows zero write affordances, keeps every pre-plan read-only workflow green, and receives clean structured rejections from every mutating API route both with a valid session token and (401) without one (F122); a writable session edits and commits a row (SQL grid on one engine, MongoDB document editor), runs a write statement through the destructive-confirmation flow, and creates + drops a table (where the engine supports it) end-to-end. Docs consolidation lands with it: README feature matrix, ARCHITECTURE.md's adding-an-engine recipe gains the capabilities/mutations/ddl/admin steps, and docs/SECURITY.md's write rules are updated from aspirational to implemented contract. Move plan 0006 to exec-plans/completed with evidence. Depends on all prior plan slices.",
      "verification": "pnpm test:e2e:full && pnpm check:state",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    }
  ]
}
