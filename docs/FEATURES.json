{
  "version": 1,
  "description": "Machine-readable live work queue. Passing entries are retained for 24 hours, then removed by pnpm features:prune; durable behavior and evidence remain in product specs and Git/PR history. See docs/FEATURES.md for rules.",
  "nextIds": {
    "F": 147,
    "DF": 13
  },
  "states": ["not_started", "active", "blocked", "passing"],
  "features": [
    {
      "id": "F146",
      "behavior": "DataGrip-style grid editing: single-click whole-cell selection; double-click/Enter/F2 inline editing for scalar values; shared True/False boolean and enum selectors; lossless date, time, time-with-time-zone, timestamp, and PostgreSQL interval editing; automatic dismissal of any active scalar, structured, or inserted-row editor when another body cell is clicked; compact right-side drawer editing for JSON, supported arrays, binary hex, XML, and intervals, with visible Format/Minify/Copy and fixed actions; grouped binary hex with byte count and ASCII preview under a friendly bytes label; native JSON/array containment filters where each engine supports them; validated PostgreSQL bit-string and network editing; anchored SET editing with optional full-panel expansion; spreadsheet keyboard navigation, typed copy/paste, nullable clearing, undo, staged dirty-state feedback, and page-level commit; automatic catalog/row refresh after successful SQL writes; fixed-width truncated columns; adaptive icon-only shell tabs and row actions below the desktop breakpoint; and a non-executing SQL Explain action with no Analyze control.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/core typecheck && pnpm --filter @qyre/ui typecheck && pnpm --filter @qyre/web typecheck && pnpm --filter @qyre/ui build && pnpm --filter @qyre/web build && pnpm --filter @qyre/qyre build && pnpm verify:pr",
      "state": "active",
      "commitHash": null,
      "evidence": "Draft PR #160 (https://github.com/ensp1re/qyre/pull/160), round 8 pushed as bdf2667. Rounds 3-8 live-review refinements: true whole-cell selection/edit ring; borderless scalar input; shared True/False and enum selectors; nullable clearing without a NULL chip; plain URL display; deterministic long-text truncation; centered loading states; adaptive labels below 1024px; every grid editor dismisses on a different body-cell click; lossless temporal and PostgreSQL interval editing; SQL Analyze UI removed and Explain always non-executing; compact right-side editing for JSON, supported arrays, bytea/blob/BLOB hex, XML, and interval values; restored JSON Format/Minify/Copy; grouped binary hex with byte count and ASCII preview; validated PostgreSQL bit/bit varying and network text; native structured containment for PostgreSQL JSON/arrays, MySQL JSON, and MongoDB objects/arrays; and immediate catalog/row refresh after successful non-read SQL. Focused component, core/server, driver unit/live integration, and PostgreSQL browser E2E suites pass. Compact 1280x720 browser QA confirms JSON and bytes utilities plus Apply/Cancel remain visible without page scrolling. Local and pre-push `pnpm verify:pr` pass on Node 22 with 34/34 package tasks, check:state, smoke E2E (11 passed, 4 skipped), and full E2E (30 passed, 47 skipped) across PostgreSQL, MySQL, SQLite, and MongoDB. Both GitHub jobs fail before any steps because the repository's Actions credits are exhausted.",
      "blockedReason": null,
      "spec": "docs/exec-plans/active/0007-product-experience-audit-and-polish.md",
      "completedAt": null
    },
    {
      "id": "DF-12",
      "behavior": "Provide one compact, accessible control and typed-editor system for Qyre's write workflows: shared Button, IconButton, Field, Select/Combobox, and editor actions; lossless text, multiline, exact numeric/decimal, identifier, tri-state boolean, enum/set, date/time/timestamp, JSON/JSONB, supported-array, and MongoDB Extended JSON editors; explicit validation and before/after feedback; and explained read-only treatment for binary and unknown values. Preserve authoritative permission, --read-only, server-validation, driver, and transaction behavior across PostgreSQL, MySQL, SQLite, and MongoDB.",
      "verification": "pnpm --filter @qyre/core test && pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm --filter @qyre/server test && pnpm --filter @qyre/testing-conformance test && pnpm --filter @qyre/ui build && pnpm --filter @qyre/web build && node scripts/with-local-env.mjs pnpm exec playwright test e2e/row-editing-commit.spec.ts e2e/mongodb-document-editor.spec.ts e2e/read-only-mode.spec.ts",
      "state": "passing",
      "commitHash": "fc57253",
      "evidence": "Draft PR #159 (https://github.com/ensp1re/qyre/pull/159), pushed as fc57253. Added compact shared Button, IconButton, Field, custom Select/Combobox, and editor-action primitives; lossless typed text, exact numeric, identifier, boolean, enum/set, temporal, JSON/JSONB, supported-array, and MongoDB structured editors; authoritative PostgreSQL enum/array and MySQL enum/set metadata; and server validation that preserves permission/read-only/transaction gates. Unsupported binary/XML/unknown values remain read-only. Live PostgreSQL verification covered keyboard selection, JSON line/column errors, Escape focus restoration, structured inspect/edit separation, add/commit persistence, and contained editor geometry at 848px and 640px. Focused PostgreSQL row commit, MongoDB document, read-only, and structured inspector E2E passed. Full local and pre-push pnpm verify:pr passed (34/34 package tasks, 11 smoke E2E passed/4 expected skips, 29 full E2E passed/43 expected skips). Both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/exec-plans/active/0007-product-experience-audit-and-polish.md",
      "completedAt": "2026-07-15T18:21:24.000Z"
    },
    {
      "id": "DF-11",
      "behavior": "Protect SQL row-editing integrity and make scalar activation predictable: define a mutation-specific editor capability matrix; fail closed for time and timestamp values until a lossless editor is verified; keep exact temporal values display-only; make row-background clicks select while editable cell controls activate consistently by pointer, Enter, or F2; and keep scalar editors overlaid so activating them does not resize rows or columns. Preserve permission, --read-only, transaction, and cross-engine behavior; MongoDB remains on its document editor.",
      "verification": "pnpm --filter @qyre/core build && pnpm --filter @qyre/core test && pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm --filter @qyre/ui build && pnpm --filter @qyre/web build && node scripts/with-local-env.mjs pnpm exec playwright test e2e/row-editing-commit.spec.ts e2e/read-only-mode.spec.ts",
      "state": "passing",
      "commitHash": "a6330fa",
      "evidence": "Merged PR #158 (https://github.com/ensp1re/qyre/pull/158), pushed as a6330fa. Added a mutation-specific editor capability matrix, failed closed for time/timestamp values the previous minute-only control could not round-trip, made scalar editing activate consistently by click/Enter/F2, restored focus on Escape, separated cell actions from row selection, and overlaid editors without changing grid geometry. Focused core, UI, and web suites passed (69, 371, and 148 tests); UI/web typechecks and builds passed; focused writable/read-only E2E passed. Live Postgres verification at 1440, 1024, and 768px confirmed unchanged 29.25px row geometry, no page overflow, no console errors, F2 activation, Escape focus restoration, and no row selection from cell activation. Full local and pre-push pnpm verify:pr passed (34/34 package tasks, 11 smoke E2E passed/4 expected skips, 29 full E2E passed/43 expected skips).",
      "blockedReason": null,
      "spec": "docs/exec-plans/active/0007-product-experience-audit-and-polish.md",
      "completedAt": "2026-07-15T17:19:49.000Z"
    },
    {
      "id": "DF-10",
      "behavior": "Audit Qyre's complete web experience before changing production code: exercise first-use and returning-user flows, all four engines, writable/read-only/restricted sessions, dense data, destructive and recovery states, keyboard/accessibility behavior, light/dark themes, and responsive desktop widths; record evidence-backed findings, systemic causes, and an approval-gated product-wide polish plan with bounded implementation slices and acceptance criteria.",
      "verification": "pnpm check:state",
      "state": "passing",
      "commitHash": "c24b1b3",
      "evidence": "Draft PR #157 (https://github.com/ensp1re/qyre/pull/157), pushed as c24b1b3. Completed a live, evidence-backed audit across all four engines, writable/read-only/restricted sessions, both themes, dense and failure states, and 320-1440px widths; captured 35 screenshots and recorded 23 findings. User review triggered a deeper editing audit that confirmed conflicting selection/edit gestures, unstable Add/Commit UI, missing structured editors and shared controls, redundant writable status, and a critical minute-only temporal mutation fidelity risk. The user approved the revised nine-slice plan. Verified with pnpm verify:pr locally and through pre-push (34/34 package tasks, 11 smoke E2E passed/4 expected skips, 29 full E2E passed/43 expected skips); both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/exec-plans/active/0007-product-experience-audit-and-polish.md",
      "completedAt": "2026-07-15T16:44:44.000Z"
    },
    {
      "id": "F137",
      "behavior": "Close SUGGESTIONS.md V4: a nullable primary-key value (legal in SQLite non-INTEGER PKs) coerces to null in resolveKey and binds into 'pk = ?' in every SQL adapter, which never matches - the mutation reports matched 0 and the UI shows the misleading 'This row was already changed or removed.' Either generate IS NULL for null key values in the adapters' key clauses, or reject null key values at validation (row-mutation-validation.ts) with an explicit 'rows with a NULL primary key cannot be targeted' message; pick one and cover it with a SQLite-backed test.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/sqlite test",
      "state": "passing",
      "commitHash": "4704f34",
      "evidence": "PR #150 (https://github.com/ensp1re/qyre/pull/150), pushed as 4704f34. Closed SUGGESTIONS.md V4 by rejecting any null primary-key member in the shared resolveKey validation path before update or delete reaches an adapter, returning 400 `Rows with a NULL primary key cannot be targeted.` instead of the misleading stale-row result produced by SQL `pk = NULL`. Added service coverage, update/delete route coverage proving adapters are not called, and a real SQLite nullable non-INTEGER primary-key fixture. Postgres, MySQL, and MongoDB are protected by the same shared validation path; the legal nullable-primary-key fixture is SQLite-specific. Verified with pnpm --filter @qyre/server test (298 tests), pnpm --filter @qyre/sqlite test (51 tests in the required non-root environment), pnpm verify:pr (34/34 check tasks, 11 smoke E2E passed/4 skipped, 29 full E2E passed/43 skipped), and both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md",
      "completedAt": "2026-07-15T12:22:29.000Z"
    },
    {
      "id": "F138",
      "behavior": "Close SUGGESTIONS.md V5 + C1 (CLI polish pair): (1) --port accepts garbage ('--port 80a0' parses to NaN) and dies with Fastify's raw 'options.port should be >= 0' error - validate the parsed flag via commander's InvalidArgumentError with a friendly message; (2) Ctrl-C during the masked password prompt calls process.exit(130) without input.setRawMode(false) (guided-login-io.ts), plausibly leaving the terminal in raw mode - restore cooked mode before exiting and verify the terminal behaves after Ctrl-C at the Password prompt.",
      "verification": "pnpm --filter @qyre/qyre test",
      "state": "passing",
      "commitHash": "8de2896",
      "evidence": "PR #151 (https://github.com/ensp1re/qyre/pull/151), merged as ddafc95. Closed SUGGESTIONS.md V5+C1: `--port` now accepts only decimal integers from 0 through 65535 and reports Commander's friendly `commander.invalidArgument` error for malformed, partial-numeric, fractional, negative, and out-of-range values; masked password prompts restore cooked terminal mode before Ctrl-C exits 130, with an ordered regression assertion proving raw mode is disabled before exit. Corrected the feature verification filter from nonexistent `@qyre/cli` to the actual `@qyre/qyre` package. Verified on Node 22 with the CLI suite (52/52), `pnpm check:quiet` (34/34 tasks), local and pre-push `pnpm verify:pr` (11 smoke E2E passed/4 skipped; 29 full E2E passed/43 skipped), and both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/CONNECTING.md",
      "completedAt": "2026-07-15T12:44:00.000Z"
    },
    {
      "id": "F141",
      "behavior": "Close SUGGESTIONS.md U3: TablesTab's openEditDocument leaves the previous document's fetch un-cancelled - opening A, closing, quickly opening B can let A's late response overwrite documentText/documentLoading while the drawer shows B's _id (the server's originalDocument check prevents a wrong write, but the user sees and edits the wrong document). Guard the resolution against the current documentEditor.id (sequence ref or AbortController) so stale responses are dropped.",
      "verification": "pnpm --filter @qyre/web test",
      "state": "passing",
      "commitHash": "3768500",
      "evidence": "Draft PR #152 (https://github.com/ensp1re/qyre/pull/152), pushed as 3768500. Closed SUGGESTIONS.md U3 with an app-owned document-load coordinator: every edit open aborts and invalidates the previous request, while closing the drawer, entering insert mode, and unmounting the Tables tab cancel the current one. The document API now accepts an AbortSignal, and text, error, and loading completions are identity-gated so an older response cannot overwrite the drawer for a newer `_id`. Added mirrored coordinator tests for replacement and close cancellation, and documented the one-active-load contract in the row-editing spec. Verified on Node 22 with the web suite (139/139), `pnpm check:quiet` (34/34 tasks), local and pre-push `pnpm verify:pr` (11 smoke E2E passed/4 skipped; 29 full E2E passed/43 skipped), and both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md",
      "completedAt": "2026-07-15T12:59:43.000Z"
    },
    {
      "id": "F142",
      "behavior": "Close SUGGESTIONS.md U6: SchemaTab hardcodes targetSchema = schemas[0], so on multi-schema Postgres databases 'New table' silently creates in whichever schema sorts first with no way to choose. Add a schema selector to CreateTableDialog (defaulting sensibly, e.g. to the sidebar's current selection when set), thread the choice through useCreateTable to POST /api/schemas/:schema/tables.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "passing",
      "commitHash": "56ac0b9",
      "evidence": "Draft PR #153 (https://github.com/ensp1re/qyre/pull/153), pushed as 56ac0b9. Closed SUGGESTIONS.md U6 by adding a controlled SQL schema selector to CreateTableDialog. SchemaTab seeds it from the current sidebar table's schema when that schema is available, otherwise from the first introspected schema, and useCreateTable now carries the chosen schema in each mutation to POST /api/schemas/:schema/tables. MongoDB's collection dialog remains unchanged. Added UI coverage for the default and selectable schema list plus a web API test proving the selected schema owns the request route. Verified on Node 22 with @qyre/ui 366/366, @qyre/web 140/140, pnpm check:quiet 34/34, local and pre-push pnpm verify:pr (11 smoke E2E passed/4 skipped; 29 full E2E passed/43 skipped), and both GitHub CI jobs passed. The first push attempt hit the repository's documented transient Postgres autocomplete /api/tables timeout; its normal retry passed the complete gate.",
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md",
      "completedAt": "2026-07-15T13:19:35.000Z"
    },
    {
      "id": "F143",
      "behavior": "Scalable-structure refactor across the ENTIRE workspace (every package and app; product source only, never test files), behavior-preserving - no functional changes. (1) Split every non-test source file over 500 lines into cohesive modules within a folder; as of 2026-07-14 that is packages/ui/src/data-grid/rows-table.tsx (933: extract toolbar, selection/drag logic, and row/cell rendering), packages/server/src/routes/schema-ddl.ts (640: split table-lifecycle, column, and index route groups), packages/ui/src/schema/table-structure.tsx (546: extract columns section, indexes section, table-actions section), and packages/ui/src/data-grid/filter-bar.tsx (517: extract filter-editor pieces) - but re-measure at execution time and split anything else that has crossed 500 lines since. (2) Group every flat folder that has grown past comfortable scanning into related subfolders, applied across the whole workspace (apps/web, packages/ui, packages/server, packages/cli, packages/core, packages/drivers/*, packages/config, packages/testing*): e.g. packages/server/src/routes (13 files) and src/services (14) by domain (rows/ddl/admin/import-export/infra), packages/ui/src/data-grid (11) and src/schema (9) by concern, and each packages/drivers/*/src (12-16 files) into introspection/mutation/admin groupings kept consistent across engines - plus any other folder found at execution time to exceed ~8 flat files. Respect docs/CODE_ORGANIZATION.md and ARCHITECTURE.md layer rules; keep each package's public exports (its index) unchanged so no consumer import breaks; pure moves/renames plus mechanical extraction only. Update docs/CODE_ORGANIZATION.md if the new folder conventions change what it documents.",
      "verification": "pnpm check:quiet",
      "state": "passing",
      "commitHash": "747e625",
      "evidence": "Draft PR #154 (https://github.com/ensp1re/qyre/pull/154), pushed as 747e625. Reorganized production source across apps/web, packages/ui, packages/server, packages/core, packages/drivers/contract, and all four engine drivers into cohesive responsibility folders while preserving package public exports and leaving every test file in place. Split the four production files over 500 lines: RowsTable into table state, toolbar, footer, export, and types modules; FilterBar into filter-editor pieces; TableStructure into row renderers; and the schema DDL route into table, column, index, and shared-support groups. Updated current structure/security/product documentation and extended the web structure check so an intentionally unmoved test must directly import its moved same-named source owner. Final audit found no production source file over 500 lines (largest 488) and no production source folder with more than 8 direct files. Verified on Node 22 with pnpm check:quiet (34/34 tasks), local and pre-push pnpm verify:pr (11 smoke E2E passed/4 skipped; 29 full E2E passed/43 skipped), and both GitHub CI jobs passed. Postgres, MySQL, SQLite, and MongoDB were covered by the repository gate.",
      "blockedReason": null,
      "spec": "docs/CODE_ORGANIZATION.md",
      "completedAt": "2026-07-15T13:52:50.000Z"
    },
    {
      "id": "F144",
      "behavior": "Eliminate Playwright fixture contention without discarding cross-engine parallelism. Add an automatic E2E fixture lock that serializes each test for every underlying engine fixture it touches (including read-only/restricted projects that share the writable engine database), lets different engines continue in parallel, acquires multi-engine locks in a stable order, and safely recovers locks left by dead worker processes. Route every Playwright spec through the fixture and explicitly declare the cross-engine Postgres-to-MongoDB journey. Add focused lock/mapping tests, remove the resolved reliability debt, and document the enforced isolation contract.",
      "verification": "pnpm --filter @qyre/testing test && pnpm --filter @qyre/web build && node scripts/with-local-env.mjs pnpm exec playwright test --grep @full --repeat-each=3",
      "state": "passing",
      "commitHash": "d9618bd",
      "evidence": "Draft PR #155 (https://github.com/ensp1re/qyre/pull/155), pushed as d9618bd. Added an automatic full-test Playwright fixture lock that maps writable, read-only, and restricted projects to their shared Postgres, MySQL, SQLite, or MongoDB fixture while retaining parallel execution between different engines. Multi-engine journeys acquire sorted locks, dead-worker locks are reclaimed, all 12 E2E specs use the shared fixture, and the resolved reliability debt was removed and documented. Verified on Node 22 with @qyre/testing tests (15/15), a no-retry full-E2E repeat-each=3 stress run (87 passed/129 expected project skips), pnpm check:quiet (34/34 tasks), local and pre-push pnpm verify:pr (11 smoke E2E passed/4 skipped; 29 full E2E passed/43 skipped), and both GitHub CI jobs passed. Postgres, MySQL, SQLite, and MongoDB fixture mappings and live suites were covered.",
      "blockedReason": null,
      "spec": "docs/RELIABILITY.md",
      "completedAt": "2026-07-15T14:54:37.000Z"
    },
    {
      "id": "F145",
      "behavior": "Close the final active review/debt slice by making de-emphasized UI text WCAG AA compliant in both themes without flattening the IDE visual hierarchy. Add a documented semantic quiet-foreground token for light and dark surfaces, replace opacity-modified muted text/placeholder classes, and re-enable Axe color-contrast checks for disconnected and connected screens in both themes. Remove the resolved accessibility debt entry. Retire docs/SUGGESTIONS.md now that all F129-F143 findings are passing: repoint their live feature records to durable owning specs and remove obsolete source/handoff references before deleting the file.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm --filter @qyre/ui build && pnpm --filter @qyre/web build && node scripts/with-local-env.mjs pnpm exec playwright test e2e/smoke.spec.ts e2e/connect-and-inspect.spec.ts",
      "state": "passing",
      "commitHash": "cbcf91e",
      "evidence": "Draft PR #156 (https://github.com/ensp1re/qyre/pull/156), pushed as cbcf91e. Added documented light/dark quiet-foreground tokens, replaced all 66 opacity-modified muted text and placeholder usages, and adjusted semantic foreground/accent tokens so the affected UI clears WCAG AA without flattening its hierarchy. Axe color-contrast coverage now runs for disconnected and connected screens in both themes. Removed the resolved accessibility debt, deleted docs/SUGGESTIONS.md, and repointed F129-F143 to durable owning specs. The requested standalone package contrast guard was removed; contrast remains exercised by the real browser accessibility scans. Verified on Node 22 with @qyre/ui 366/366, @qyre/web 140/140, focused two-theme E2E 11 passed/4 expected skips, pnpm check:quiet 34/34, local and both pre-push pnpm verify:pr runs (11 smoke E2E passed/4 skipped; 29 full E2E passed/43 skipped), and both GitHub CI jobs passed.",
      "blockedReason": null,
      "spec": "docs/references/design-system.md",
      "completedAt": "2026-07-15T15:32:47.000Z"
    }
  ]
}
