{
  "version": 1,
  "description": "Machine-readable live work queue. Passing entries are retained for 24 hours, then removed by pnpm features:prune; durable behavior and evidence remain in product specs and Git/PR history. See docs/FEATURES.md for rules.",
  "nextIds": {
    "F": 129,
    "DF": 10
  },
  "states": ["not_started", "active", "blocked", "passing"],
  "features": [
    {
      "id": "F122",
      "behavior": "Local-server authentication and browser hardening, landing FIRST in plan 0006 because it protects even the read-only product and is a precondition for any mutating route: the CLI mints a cryptographically random per-session token at startup, the served UI receives it (injected into index.html or fetched via a one-time bootstrap), and every /api route rejects requests without it (401, structured error) - closing the current no-auth surface where any local process/user can call the full API and where cross-origin CSRF is blocked only implicitly by Fastify's JSON content-type requirement (pgweb/DbGate precedent for local web DB tools). Security headers land with it: CSP on the served SPA (also neutralizes DB-data-driven request exfil via F086's image URL previews), X-Content-Type-Options, X-Frame-Options. Open decision (exec plan 0006 #3): header-based token + fetch/blob downloads vs cookie - the CSV export links (plain <a href>) must keep working, so the choice must cover download URLs. E2E: UI works end-to-end with the token; a tokenless curl to every /api route 401s; docs/SECURITY.md updated. No dependency on other plan slices.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter qyre test && pnpm test:e2e",
      "state": "passing",
      "commitHash": "339010d85e1b0555ad9cb548bb47d30c60aabf08",
      "evidence": "PR #94 (https://github.com/ensp1re/qyre/pull/94), pushed as 339010d. Added @qyre/server/src/services/auth-token.ts (crypto.randomBytes token, constant-time compare) and two new plugins: auth-guard.ts (rejects any /api/* request without a valid token - Authorization: Bearer header, or a ?token= query param for the CSV export's plain <a href> download, which can't set headers) and security-headers.ts (CSP with connect-src/script-src/default-src locked to 'self' but img-src open to http/https for the F086 previews, plus X-Content-Type-Options: nosniff and X-Frame-Options: DENY). createServer generates a random token per instance (or accepts one via options for tests) and decorates it onto the FastifyInstance; static-web.ts now serves index.html dynamically (explicit / and /index.html routes plus the SPA-fallback notFoundHandler) with the token injected as window.__QYRE_TOKEN__, replacing @fastify/static's raw index-file serving. apps/web's fetchJson attaches the token as an Authorization header (read via a new shared/api/auth-token.ts); exportRowsUrl appends it as a query param for the real-navigation CSV download. Resolved a cross-package typecheck gap along the way: a standalone types/fastify.d.ts augmentation file wasn't visible when @qyre/qyre's own tsc run pulled in server/src/app.ts transitively (each package's tsconfig only includes its own src) - moved the `declare module \"fastify\"` block inline into app.ts, which is always part of every consumer's compiled unit. Updated all ~59 existing app.inject call sites across 8 test files to send an auth header via a new tests/helpers/auth.ts; added auth-guard.test.ts, security-headers.test.ts, token-injection cases in static-web.test.ts, and e2e/auth-token.spec.ts (tokenless/invalid-token 401s, real browser UI still works end-to-end - all 4 engine projects). docs/SECURITY.md updated. Verified: pnpm --filter @qyre/server test (83 passed), pnpm --filter qyre test / pnpm --filter @qyre/qyre test (44 passed), pnpm test:e2e, pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": null,
      "completedAt": "2026-07-10T16:52:22.000Z"
    },
    {
      "id": "F090",
      "behavior": "Product-spec pass for the role-aware permission/capability model before any implementation: create docs/product-specs/permissions-and-capabilities.md covering the two-tier model (session-level ConnectionCapabilities extending F063's AdapterCapabilities with supportsRowMutations/supportsDdl/supportsIndexManagement/supportsDatabaseManagement/supportsTransactions plus a readOnlyReason of qyre-flag|replica|connection|grants|null, and per-table select/insert/update/delete TablePermissions attached to TableMetadata), the per-engine introspection matrix (Postgres has_table_privilege/pg_is_in_recovery/default_transaction_read_only, MySQL information_schema privileges + role grants + @@global.read_only, SQLite file+directory writability/PRAGMA query_only/mode=ro, MongoDB connectionStatus showPrivileges action sets), the advisory-introspection/authoritative-database principle with degradation to read-only on introspection failure, the API-shape decision (extend GET /api/overview vs a new endpoint - leaning extended overview), and how the UI adapts per access level. Follow the spec pattern of F063-F066. Architectural decisions are pre-settled in docs/exec-plans/active/0006-role-aware-database-ide.md; this spec turns them into a reviewable product contract with acceptance criteria.",
      "verification": "pnpm check:state",
      "state": "passing",
      "commitHash": "32affa0b4a619b9904d70607ec8925a79805ebd1",
      "evidence": "PR #95 (https://github.com/ensp1re/qyre/pull/95), pushed as 32affa0. Added docs/product-specs/permissions-and-capabilities.md: the two-tier ConnectionCapabilities (session-level, extends F063's AdapterCapabilities with supportsRowMutations/supportsDdl/supportsIndexManagement/supportsDatabaseManagement/supportsTransactions/readOnlyReason) / TablePermissions (per-table select/insert/update/delete, optional on TableMetadata) data contract, the per-engine introspection matrix (which native mechanism each of Postgres/MySQL/SQLite/MongoDB reads for each tier, plus each engine's restricted test fixture), and the advisory-introspection/authoritative-database principle (database always the real enforcer; introspection failure degrades to read-only never assumed-writable; --read-only always wins). Resolved exec plan 0006's open decision #4: ConnectionCapabilities travels inside the existing GET /api/overview response, not a new endpoint. Registered in docs/product-specs/index.md. Spec-only slice, no code changes - acceptance criteria reflect that (F091 implements the types). Verified: pnpm check:state, pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB, unaffected by this docs-only change). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-10T17:10:38.000Z"
    },
    {
      "id": "F091",
      "behavior": "Capability plumbing with no real introspection yet: @qyre/core gains the ConnectionCapabilities and TablePermissions types per docs/product-specs/permissions-and-capabilities.md (F090); DatabaseAdapter (packages/drivers/contract) gains a required getCapabilities(): Promise<ConnectionCapabilities>; all four adapters return their static engine truths (supportsSql as today, MongoDB column-DDL not-applicable, etc.) with the write/session tier hardcoded to read-only until F092-F095 land; the server exposes capabilities on the agreed API surface and re-reports them after POST /api/connect swaps the adapter (F064/F085); apps/web gains one shared capabilities hook in features/connection that all later write-UI gating consumes. A @qyre/testing-conformance case asserts every registered engine reports a well-formed capabilities object. No visible UI change. Depends on F090.",
      "verification": "pnpm --filter @qyre/core test && pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "050e92c13ff74bdac0f4a656d3ccdde80d4b4132",
      "evidence": "PR #96 (https://github.com/ensp1re/qyre/pull/96), pushed as 050e92c. @qyre/core gains ConnectionCapabilities (extends F063's AdapterCapabilities) and TablePermissions per docs/product-specs/permissions-and-capabilities.md; DatabaseOverview.capabilities is now ConnectionCapabilities and TableMetadata gains optional permissions?: TablePermissions. DatabaseAdapter (packages/drivers/contract) gains a required getCapabilities(): Promise<ConnectionCapabilities>; a new shared stubReadOnlyCapabilities(supportsSql) helper gives every adapter (Postgres/MySQL/SQLite/MongoDB) an interim implementation - every write flag hardcoded false, readOnlyReason 'grants' - until each engine's own introspection slice (F092-F095) lands; each adapter's getOverview() now calls its own getCapabilities() instead of inlining a literal. GET /api/overview exposes the full ConnectionCapabilities shape (resolves F090's open decision #4 - no new endpoint; re-reported automatically after POST /api/connect swaps the adapter since getOverview() is refetched fresh). apps/web gains features/connection/model/use-capabilities.ts, the shared hook later write-UI gating consumes - shares the useOverview/['overview'] React Query cache without a cross-feature import (features can't import each other per apps/web/STRUCTURE.md) so it costs no duplicate fetch. A @qyre/testing-conformance case asserts every registered engine reports a well-formed capabilities object, live against all 4 engines. No visible UI change. Found along the way: vitest resolves workspace packages through their built dist/, not source, so @qyre/core/@qyre/driver-contract/all four adapters needed a rebuild mid-session before tests could see the new exports. Verified: pnpm --filter @qyre/core test (41 passed), pnpm --filter @qyre/server test (84 passed, incl. a new GET /api/overview capabilities-shape assertion), pnpm --filter @qyre/testing-conformance test (44 passed), pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-10T17:47:16.000Z"
    },
    {
      "id": "F123",
      "behavior": "Batched schema introspection, landing BEFORE per-engine permission work multiplies the current fan-out: GET /api/tables today calls getTable per table - each >= 4 catalog queries plus a row-count estimate (with a possible exact COUNT(*) fallback) - through an unbounded Promise.all (packages/server/src/routes/tables.ts), which hammers large databases and was already tracked as tech debt. Adapters gain a batched all-tables metadata path (e.g. getAllTables(): one/few set-based catalog queries per engine - information_schema joins on Postgres/MySQL, sqlite_master + per-table pragmas with bounded concurrency on SQLite, listCollections + capped sampling on MongoDB); the route uses it; getTable stays for single-table use. Row-count estimates become part of the batched query (pg_class.reltuples, information_schema.TABLES.TABLE_ROWS) rather than per-table round trips. Conformance asserts the batched path returns the same shape as N x getTable on every engine. F092-F095 attach per-table permissions to this batched path. Depends on F091.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "183cb3ab2d65911de2863b8ea83009270a457238",
      "evidence": "PR #98 (https://github.com/ensp1re/qyre/pull/98), pushed as 183cb3a. DatabaseAdapter gains getAllTables(): TableMetadata[] in the same shape N getTable() calls would produce. Postgres/MySQL each get one batched query for columns, PK/FK, and indexes across every table (information_schema, no per-table filter); row counts batch too - Postgres keeps getTable()'s reltuples-estimate-with-exact-fallback semantics exactly (one estimate query, then a targeted exact COUNT(*) only for never-ANALYZEd tables), MySQL uses a single UNION ALL of exact COUNT(*) per table rather than the originally-suggested information_schema.TABLES.TABLE_ROWS estimate, since that can lag right after insert and would break exact parity with getTable() (a documented engineering deviation from the feature's literal text, made for correctness/determinism). SQLite/MongoDB have no cross-table catalog query for either engine (pragmas and field-sampling are inherently per-table/collection) - the batching win there is moving the fan-out from the route's unbounded Promise.all into a bounded sequential loop inside the adapter that reuses getTable(), correct by construction. GET /api/tables (packages/server/src/routes/tables.ts) now calls getAllTables() directly instead of getOverview() + Promise.all(getTable). A @qyre/testing-conformance case asserts the batched path returns the exact same shape as calling getTable() per table, live against all 4 engines. Verified: pnpm --filter @qyre/server test (84 passed), pnpm --filter @qyre/testing-conformance test (48 passed, incl. the new parity test), each adapter's own test suite unchanged, pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB - Schema tab exercises getAllTables() end-to-end). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": null,
      "completedAt": "2026-07-10T18:15:58.000Z"
    },
    {
      "id": "F124",
      "behavior": "Tables and views become distinct kinds - a modeling gap the write stage would trip over: all three SQL drivers read information_schema.tables (or sqlite_master) with no table_type filter today, so views appear as ordinary editable 'tables'. TableMetadata (and the batched F123 path) gains kind: 'table' | 'view' | 'materialized-view' | 'collection' (Postgres pg_class.relkind/table_type incl. matviews - which information_schema misses - MySQL TABLE_TYPE, SQLite sqlite_master.type, MongoDB collection vs view from listCollections). The UI shows a subtle kind badge in the sidebar/schema views; every later mutation/DDL surface gates on kind (editing a view's rows, DROP TABLE on a view, truncating a matview are all prevented up front with the reason stated). Read-only behavior otherwise unchanged. Conformance: a fixture view per SQL engine (and a MongoDB view) reports its kind correctly everywhere. Depends on F123.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test && pnpm --filter @qyre/ui test",
      "state": "passing",
      "commitHash": "2ac72f1e91d361759008deb453c4520a89c0cea8",
      "evidence": "PR #99 (https://github.com/ensp1re/qyre/pull/99), pushed as 2ac72f1. TableMetadata gains a required kind: 'table' | 'view' | 'materialized-view' | 'collection'. Fixed two real visibility gaps beyond mistagging: Postgres materialized views were entirely invisible before (information_schema.tables has no concept of them - now sourced from pg_class directly, relkind IN ('r','p','f','v','m')); SQLite views were entirely excluded from every listing (WHERE type = 'table' only - now type IN ('table','view')). Postgres/MySQL derive kind from the same batched listing query F123 already built (pg_class.relkind / information_schema.tables.table_type), no extra round trip; SQLite from sqlite_master.type; MongoDB from listCollections' previously-discarded type field. Row counts skip views entirely across all four engines (a view's reltuples/TABLE_ROWS is meaningless with no storage of its own, and COUNT(*) would silently re-execute the underlying query). The Schema tab's TableDetail card (already receives full TableMetadata, zero plumbing) shows a subtle VIEW/MATERIALIZED VIEW badge, omitted for the unremarkable default (table/collection) per engine. Deliberately deferred: the sidebar tree badge - SchemaMetadata.tables is bare string[] today, and threading kind through it would mean changing that type across all four adapters' getOverview() output plus every existing test asserting on that shape, a materially larger change than this slice needs; the Schema tab (the primary place kind matters for future DDL/mutation gating) is covered. A @qyre/testing-conformance case creates a real view (and a MongoDB view via viewOn) per engine and asserts kind is correct via both getTable() and the batched getAllTables() path, live against all 4 engines. Verified: pnpm --filter @qyre/server test (84 passed), pnpm --filter @qyre/testing-conformance test (52 passed, incl. new view-fixture kind assertions), pnpm --filter @qyre/ui test (159 passed, incl. 4 new TableDetail badge render tests), each adapter's own test suite unchanged, pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB). GitHub CI passed both End-to-end and Lint/typecheck/test/build. Live browser screenshot of the badge wasn't possible in this environment (Claude in Chrome extension unreachable) - verified via the 4 component-level render tests instead.",
      "blockedReason": null,
      "spec": null,
      "completedAt": "2026-07-10T18:58:25.000Z"
    },
    {
      "id": "F092",
      "behavior": "Postgres permission introspection per the F090 spec: getCapabilities() reports real session writability (pg_is_in_recovery(), default_transaction_read_only, role attributes from pg_roles, has_database_privilege/has_schema_privilege CREATE checks), and getTable plus the batched F123 all-tables path attach per-table TablePermissions via has_table_privilege(current_user, ..., 'SELECT/INSERT/UPDATE/DELETE') batched per schema (one set-based query, not per-table round trips; works uniformly for tables and views). Introspection failure degrades to read-only with a logged warning, never to assumed-writable. Adds a restricted qyre_readonly Postgres user (SELECT-only grants) to the docker-compose init and @qyre/testing so integration tests cover both access levels from now on. Depends on F091, F123.",
      "verification": "node scripts/with-local-env.mjs pnpm --filter @qyre/postgres test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "3567f9e112e9d41903aef79ea029d1c11896d8b7",
      "evidence": "PR #100 (https://github.com/ensp1re/qyre/pull/100), pushed as 3567f9e. Postgres now introspects replica/session-read-only/role and CREATE-grant facts for real connection capabilities, attaches set-based per-relation privileges in both metadata paths, logs and degrades introspection failure to read-only, and provisions the qyre_readonly SELECT-only fixture. Verified: pnpm verify:pr; Postgres 38 tests; conformance 52 tests; GitHub CI Lint/typecheck/test/build and End-to-end passed.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-10T19:27:21.000Z"
    },
    {
      "id": "F093",
      "behavior": "MySQL permission introspection per the F090 spec: getCapabilities() reports session writability (@@global.read_only, @@session.transaction_read_only) and getTable/the batched F123 path attach per-table TablePermissions by unioning information_schema.USER_PRIVILEGES, SCHEMA_PRIVILEGES, and TABLE_PRIVILEGES - PLUS role-granted privileges, which those three tables omit entirely: use ROLE_TABLE_GRANTS/ROLE_SCHEMA_GRANTS (MySQL 8.0.19+) for currently-active roles, falling back to grants-only introspection (degrade toward read-only, log a warning) on older servers where role visibility is unavailable. A grant at any broader level implies the table-level right; the union must be tested explicitly since MySQL scatters grants across all these levels. Adds a restricted qyre_readonly MySQL user (SELECT-only) plus a role-granted-writer fixture user to the docker-compose init and @qyre/testing. Depends on F091, F123.",
      "verification": "node scripts/with-local-env.mjs pnpm --filter @qyre/mysql test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "1b8b286e8994d1fded82ba67a53453cb950e3909",
      "evidence": "PR #103 (https://github.com/ensp1re/qyre/pull/103), pushed as 1b8b286. Deviates from the plan's literal ROLE_TABLE_GRANTS/ROLE_SCHEMA_GRANTS approach after live verification against MySQL 8.4.10: information_schema.ROLE_SCHEMA_GRANTS does not exist, and ROLE_TABLE_GRANTS only sees exact-table role grants (misses schema-wide db.* role grants entirely). Instead, packages/drivers/mysql/src/permissions.ts parses the session's own SHOW GRANTS output, which MySQL resolves into the merged effective grant set including active default roles - no MySQL-version gate needed since SHOW GRANTS resolving default roles is standard from MySQL 8.0.0. Extracted packages/drivers/mysql/src/catalog.ts (SYSTEM_SCHEMAS/tableKey) mirroring Postgres's module structure. getCapabilities() reports real session writability from @@global.read_only/@@session.transaction_read_only plus parsed grants; getTable and the batched F123 getAllTables() path attach per-table TablePermissions via tablePermissionsFromGrants (direct table grant, schema-wide db.* grant, or role-derived grant - all resolved identically since SHOW GRANTS already merges them). Introspection failure degrades to read-only (READ_ONLY_TABLE_PERMISSIONS/stubReadOnlyCapabilities), never assumed-writable. Added qyre_readonly (SELECT-only) and qyre_role_writer (SELECT direct, INSERT/UPDATE/DELETE only via an active default role qyre_writer_role - the exact case the plan's original approach would have missed) fixtures to @qyre/testing's setupMysqlFixture and docker/mysql/init/01-qyre-readonly.sql. 12 new unit tests in packages/drivers/mysql/tests/permissions.test.ts (fake pool keyed on query text, robust to Promise.all ordering) plus 3 new integration tests including the role-derived-grant regression proof. testing-conformance's capabilities well-formedness test now covers postgres and mysql. Verified: pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB via Docker Compose, which recreated the MySQL container due to the new volume mount and exercised the init-SQL path too). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-10T22:01:45.000Z"
    },
    {
      "id": "F094",
      "behavior": "SQLite writability introspection per the F090 spec: getCapabilities() reflects whether the session can write at all - the database file AND its directory must be writable (WAL/journal files need directory write), PRAGMA query_only must be off, and a ?mode=ro/immutable open flag forces read-only; per-table TablePermissions are uniform (SQLite has no users/grants - the whole file is writable or not) and must say so via the capabilities shape rather than faking per-table variance. Tests cover a writable file, a chmod-read-only file, a read-only directory, and a mode=ro target. Depends on F091, F123.",
      "verification": "pnpm --filter @qyre/sqlite test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "82f4bd4ca075a7c3050b6a40b16debada399fe49",
      "evidence": "PR #104 (https://github.com/ensp1re/qyre/pull/104), pushed as 82f4bd4. Deviates from the plan's literal text in the same spirit as F093's SHOW GRANTS pivot: adapter.ts's connect() previously force-opened every SQLite connection with { readonly: true } unconditionally, as Qyre's own defense-in-depth policy independent of the underlying file's real permissions - under that policy db.readonly always read true, making the 'opened with a read-only flag' gate a permanent constant and the whole capabilities feature a no-op. connect() now opens normally (falling back to an explicit read-only open only if that throws outright), so db.readonly becomes a real, live signal. To keep the safety guarantee at least as strong as before, runReadOnlyQuery's authoritative backstop moved from the connection being permanently opened read-only to toggling PRAGMA query_only around each query (try/finally) - the same query-time enforcement pattern Postgres/MySQL already use via runInReadOnlyTransaction. New packages/drivers/sqlite/src/capabilities.ts (computeCapabilities/tablePermissionsFromCapabilities) gates writability on four independent, live-verified signals: file OS-writability, containing-directory OS-writability (WAL/rollback-journal sidecar files need directory write even when the file itself is writable - verified live that a writable file in a read-only directory still refuses every write), PRAGMA query_only, and the connection's own open mode. Per-table TablePermissions are uniform per file and derived directly from the session-level result, computed once and shared across getAllTables()'s batched (F123) path. readOnlyReason for a non-writable SQLite session is now 'connection' (replacing the F091 stub's 'grants', which was never accurate for an engine with no grants concept). 7 new unit tests in tests/capabilities.test.ts against real temp files/directories (writable, chmod-read-only file, read-only directory, an explicitly readonly-opened 'mode=ro' connection - better-sqlite3 has no URI-string mode=ro support, confirmed by reading its C++ source, so the boolean readonly option is the literal implementation of that concept here) plus 3 new/updated integration tests, including a direct-bypass proof of the new query_only backstop mirroring Postgres/MySQL's own such tests. testing-conformance's capabilities test gained a dedicated sqlite branch (its self-contained fixture is a normal writable tempdir file, so it now expects full writability like Postgres/MySQL, except supportsDatabaseManagement: false). Verified: pnpm --filter @qyre/sqlite test (24 passed), node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test (52 passed against live Postgres/MySQL/MongoDB + self-contained SQLite), pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB via Docker Compose). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-10T22:34:13.000Z"
    },
    {
      "id": "F095",
      "behavior": "MongoDB permission introspection per the F090 spec: getCapabilities() and per-collection TablePermissions derive from connectionStatus{showPrivileges:true} - mapping find/insert/update/remove and createCollection/dropCollection/createIndex/dropIndex actions per db/collection resource (including anyResource and db-level wildcards); an unauthenticated local connection (no authenticatedUsers) is full-access, matching mongod's real behavior; introspection failure degrades to read-only with a logged warning. Adds a restricted fixture user with the built-in read role on the test db to the mongo init and @qyre/testing. Depends on F091, F123.",
      "verification": "node scripts/with-local-env.mjs pnpm --filter @qyre/mongodb test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "passing",
      "commitHash": "b6b5e4fce6d559a4bbbf6a4efb7009a5fefd2a36",
      "evidence": "PR #105 (https://github.com/ensp1re/qyre/pull/105), pushed as b6b5e4f. getCapabilities()/per-collection TablePermissions derive from db.runCommand({connectionStatus:1, showPrivileges:true}) as specified, mapping find (select) and insert/update/remove (row mutations) plus createCollection/dropCollection (supportsDdl) and createIndex/dropIndex (supportsIndexManagement) per resource - exact db+collection, db-level wildcard (collection:''), cross-db wildcard (db:''), and anyResource:true, all shapes live-verified against a real mongod by creating read/readWrite/dbAdmin/custom-anyResource users and capturing their exact connectionStatus output as unit-test fixture data. System databases (admin/local/config) and system.* collections are excluded from the session-level aggregate checks, matching introspectSchemas' existing listing filter (now shared via a new catalog.ts, mirroring the other three adapters' module split). An unauthenticated connection (empty authenticatedUsers) is treated as full access, live-verified as mongod's actual default when authorization isn't enabled. supportsDatabaseManagement and supportsTransactions are always false for this slice (deviation, documented in permissions.ts's top comment): MongoDB has no paired create/drop-database privilege, and real multi-document transactions additionally require replica-set topology a standalone mongod can't provide regardless of grants - both out of scope for a privilege-only introspection slice. Introspection failure degrades to read-only, logged. Scoping decision confirmed with the user before implementing: rather than the plan's literal 'adds a restricted fixture user to the mongo init', testing a genuinely restricted user live would require enabling MongoDB authorization globally on the shared docker-compose/CI container, breaking every existing anonymous Mongo test/fixture/e2e spec - a disproportionate blast radius versus F092-F094's purely additive restricted fixtures. Restricted-access scenarios are instead covered by 10 unit tests against the live-verified connectionStatus shapes; the live integration test proves the unauthenticated-full-access path end-to-end against the real, unchanged container. A pre-existing MongoDB-only safety test (scanning adapter source for accidental write-API calls) needed scoping to exclude permissions.ts, which legitimately contains MongoDB's own privilege action-name vocabulary (createCollection/createIndex/etc.) as string literals, not method calls - added a second, stricter test that only flags actual `methodName(` call syntax in that file. Verified: node scripts/with-local-env.mjs pnpm --filter @qyre/mongodb test (50 passed), node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test (52 passed against live Postgres/MySQL/MongoDB + self-contained SQLite), pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-11T10:22:01.000Z"
    },
    {
      "id": "F096",
      "behavior": "Qyre-level read-only session mode as the safety cornerstone, landing BEFORE any mutating route exists: the CLI gains a --read-only flag (mentioned in --help and the F088 guided-login flow) that forces the whole session read-only regardless of database grants; the server stores the mode in ServerContext and one central guard (a single choke point every future mutating route must register under, e.g. a route-config flag checked in a shared preHandler) rejects any mutating request with a structured error; capabilities report readOnlyReason 'qyre-flag' so the UI can explain itself; POST /api/connect preserves the flag across connection switches. Depends on F091, F122.",
      "verification": "pnpm --filter qyre test && pnpm --filter @qyre/server test",
      "state": "passing",
      "commitHash": "97bee3a44a7593eb7b7820640c822d4f21445af2",
      "evidence": "PR #106 (https://github.com/ensp1re/qyre/pull/106), pushed as 97bee3a. The CLI gains a --read-only flag (packages/cli/src/index.ts's parseArgs, listed in --help alongside --login) that forces the whole session read-only regardless of what the connected database role would allow. ServerContext gains a readonly readOnly: boolean, set once at createServer() time; POST /api/connect never touches it, so it persists across connection switches by construction (verified with a live adapter swap in a new overview.test.ts case). GET /api/overview's capabilities are overridden after the adapter's own getCapabilities() resolves (packages/server/src/services/read-only-capabilities.ts's applyReadOnlyOverride) - every supports* flag false, readOnlyReason 'qyre-flag', proven to win over a fake adapter that reports full writability. New packages/server/src/plugins/read-only-guard.ts registers the single central choke point every future mutating route must opt into via a Fastify route-config flag (config: { mutating: true }, read via request.routeOptions.config.mutating in a shared preHandler) - a no-op today since no mutating route exists yet, tested by registering throwaway test routes directly against the built app. Minor documented deviation: the spec's 'without even running the introspection queries' optimization is not pursued (would require reshaping every adapter's getOverview()/getCapabilities() for a marginal perf saving on a local-only tool) - the override applies to the already-computed result instead, which is behaviorally identical to every caller. Documented in docs/SECURITY.md as the enforcement mechanism every future write route must register under. Verified: pnpm --filter @qyre/qyre test (46 passed), pnpm --filter @qyre/server test (94 passed), pnpm check:quiet, pnpm verify:pr (full local gate incl. @full E2E against live Postgres/MySQL/SQLite/MongoDB). GitHub CI passed both End-to-end and Lint/typecheck/test/build.",
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md",
      "completedAt": "2026-07-11T10:52:31.000Z"
    },
    {
      "id": "F097",
      "behavior": "Permission-aware UI shell: apps/web consumes the F091 capabilities hook - the status/title bar shows the session access level (read-only vs read-write badge) with a tooltip explaining why (Qyre --read-only flag, replica, read-only connection, grants), and capability-gating helpers land in features/connection for every later write surface to reuse. A new Playwright e2e asserts a read-only session (restricted fixture user and --read-only flag) renders zero write affordances - this is the standing regression guard every later write slice must keep green. Today's read-only visual experience is otherwise unchanged. Depends on F092-F096.",
      "verification": "pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/permissions-and-capabilities.md"
    },
    {
      "id": "F098",
      "behavior": "Product-spec pass for row-level editing before implementation: create docs/product-specs/row-editing.md covering the structured mutation API (typed JSON translated to parameterized SQL or MongoDB collection ops inside each adapter - the F072 filter pattern; the browser never builds SQL), row identity rules (full primary key required, composite keys supported, MongoDB uses _id; PK-less tables and any kind !== 'table'/'collection' (F124 views/matviews) stay read-only with the reason surfaced), and the two editing surfaces settled in exec plan 0006: SQL engines use grid editing with a pending-changes buffer + generated-statement preview + transactional batch commit; MongoDB uses whole-document Extended JSON editing (Compass model) because flat cell edits cannot express nested documents and display values are already serialized (dates as ISO strings per F081, ObjectId as hex) - the spec must fix the EJSON flavor (relaxed vs canonical) and changed-fields-update vs document-replace semantics (exec plan 0006 open decision #1, the highest-risk decision in the plan). Also: value validation/coercion per column type reusing F082/F089 capability classification, the commit endpoint shape (open decision #5), the audit-event contract, and confirmation thresholds per docs/SECURITY.md. Depends on F090, F124.",
      "verification": "pnpm check:state",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F099",
      "behavior": "Structured row insert across all four engines per docs/product-specs/row-editing.md: the adapter mutations namespace gains insertRow - SQL engines take a flat column->value map translated to a parameterized INSERT; MongoDB takes an Extended JSON document parsed server-side to real BSON (per the F098 spec's EJSON decision) passed to insertOne - with value coercion per column type and the inserted row/key reported where the engine supports it. The server adds POST /api/tables/:schema/:table/rows, validating the body with Zod against the table's real introspected columns (same injection-surface pattern as F065/F072), rejecting kind !== 'table'/'collection' targets (F124), gated by both the F096 central guard and the table's insert permission, and logging a structured audit event (EventLog + pino, per exec plan decision 8). Conformance cases cover successful insert plus rejections (read-only session, unknown column, view target, restricted fixture user) on all four engines. Depends on F096, F098.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F100",
      "behavior": "Structured row update per docs/product-specs/row-editing.md: the mutations namespace gains updateRowByKey requiring a full primary-key match (composite keys supported; MongoDB matches _id), rejecting tables without a primary key and kind !== 'table'/'collection' targets, parameterized everywhere; SQL engines take a flat changes map; MongoDB takes the F098-specced document-update payload (changed-fields or replace, per the spec decision) parsed from EJSON server-side. Zero-rows-matched is reported as a distinct outcome (stale row), never as silent success. Server route (PATCH on the rows resource) gated by guard + update permission, audited, with the same column-validation pattern as F099. Conformance across all four engines including restricted-user rejection and the 0-matched outcome. Depends on F099.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F101",
      "behavior": "Structured row delete per docs/product-specs/row-editing.md: the mutations namespace gains deleteRowsByKey(schema, table, keyMatches[]) deleting only by an explicit list of primary-key identities (no filter-based bulk delete in this slice - that stays a deliberate non-feature until a spec revisits it), parameterized everywhere, MongoDB by _id list, kind-gated (F124). Server route gated by guard + delete permission, audited with the deleted count; zero-matched keys reported distinctly. Conformance across all four engines including restricted-user rejection. Depends on F100.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F102",
      "behavior": "Batch commit for the SQL pending-changes model per docs/product-specs/row-editing.md: one endpoint applies an ordered set of staged inserts/updates/deletes wrapped in a single transaction on Postgres/MySQL/SQLite (all-or-nothing; rollback on first failure with the failing op index reported). MongoDB is deliberately NOT part of this endpoint - its document editor (F125) saves per-document, per the exec plan's decision 5. Adapters gain the transactional wrapper in their mutations namespace; the server validates every op against introspected columns/permissions/kind before starting, and audits the batch outcome. Conformance: mixed-batch success and mid-batch failure rollback on the three SQL engines; MongoDB asserts the endpoint cleanly reports not-applicable. Depends on F101.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F103",
      "behavior": "The Rows table becomes an editable grid on SQL engines when - and only when - session capabilities, the table's permissions, its kind (F124: real tables only, not views), and a present primary key allow: double-click/Enter starts inline cell editing with type-aware editors reusing the F082/F089 filter value controls (boolean picker, native date/time/datetime inputs, number/text, explicit NULL setter, revert-cell), edits stage into a visible pending-changes buffer (dirty-cell styling) without touching the server, and editing is cleanly disabled - with the reason surfaced in UI copy - for read-only sessions, PK-less tables, views, and non-editable columns per the spec. MongoDB does not participate (its editing surface is F125's document editor). The buffer lives in features/table model state; commit wiring arrives in F105. Depends on F097, F098.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F104",
      "behavior": "Permission-gated Add-row and Duplicate-row affordances for the SQL editable grid per docs/product-specs/row-editing.md: a new-row editor with the same type-aware inputs as F103, nullable/default-value handling, and per-column validation before staging into the pending buffer; Duplicate pre-fills from the selected row minus auto-generated keys (serial/identity) per the spec. Hidden entirely for sessions/tables without insert permission, views, and MongoDB (F125 owns document insertion). Depends on F103.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F105",
      "behavior": "The SQL pending-changes workflow completes per docs/product-specs/row-editing.md: selected rows (F083 selection) can be staged for deletion when permitted; a commit bar shows staged insert/update/delete counts, previews the exact generated parameterized statements before applying (TablePlus commit-preview model), commits through the F102 batch endpoint with per-op error surfacing (failed op highlighted, buffer preserved on failure so work is never lost), and discards cleanly; deletes above the spec's threshold require typed confirmation. E2E: an edit-commit-verify roundtrip on one SQL engine, and the F097 read-only guard stays green. Depends on F102, F104.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F125",
      "behavior": "MongoDB document editing per docs/product-specs/row-editing.md (Compass model, the F098 spec's EJSON decisions): the existing cell-value/row inspection flow gains an Edit-document mode rendering the full document as Extended JSON in an editor with validation; save performs the specced changed-fields update or document replace via the mutations namespace (server-side EJSON parse to real BSON - ObjectId/Date/Decimal128 survive the round trip; a lossy edit of a type the editor cannot represent must be rejected, not silently coerced), with a confirmation step showing the operation before it runs; insert-document and delete-document (typed confirmation) complete the surface. Permission-gated by F095's introspection, kind-gated (collections only, not Mongo views), audited. E2E: edit-save-verify a nested field and an ObjectId-preserving roundtrip against the MongoDB fixture; the F097 read-only guard stays green. Depends on F100, F101, F097.",
      "verification": "node scripts/with-local-env.mjs pnpm --filter @qyre/mongodb test && pnpm --filter @qyre/ui test && pnpm test:e2e",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/row-editing.md"
    },
    {
      "id": "F106",
      "behavior": "packages/drivers/contract gains classifyStatement(sql) labeling a single SQL statement as read | mutation | ddl | destructive (DROP/TRUNCATE always destructive; UPDATE/DELETE without a WHERE clause destructive; writable CTEs classify as their write action), extracted by generalizing assertReadOnly's existing comment/literal-stripping scanner so read-only behavior stays bit-for-bit unchanged - assertReadOnly is reimplemented on top of the classifier and its existing tests pass untouched. Pure text heuristic, defense-in-depth only; the authoritative gate remains engine-level enforcement. Unit tests cover the classification matrix including comment/literal/CTE edge cases. No server or UI change. Depends on F090.",
      "verification": "pnpm --filter @qyre/driver-contract test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F107",
      "behavior": "Write-capable SQL execution per an updated docs/product-specs/sql-editor.md: Postgres/MySQL/SQLite adapters gain runQuery(sql) executing any single statement without the READ ONLY transaction wrapper, returning rows or an affected-row count, honoring existing timeouts and the F050 result cap (MongoDB keeps supportsSql=false, untouched). CRITICAL: the write path must NOT apply Postgres's coerceUnknownQuotedIdentifiers rewrite (packages/drivers/postgres) - that DWIM double-quote-to-string coercion is acceptable on reads but must never silently alter a mutation's SQL; a regression test locks this in. POST /api/query keeps runReadOnlyQuery exactly as today for read-only sessions; write-capable sessions route through classifyStatement (F106) - mutation/ddl statements run directly, destructive ones are rejected unless the request carries an explicit confirmed flag (a server-enforced round-trip, never client-only). Every non-read statement logs an audit event with its classification and affected rows. Server tests + conformance cover each class, unconfirmed-destructive rejection, the coercion bypass, and read-only-session behavior unchanged. Depends on F096, F106.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/sql-editor.md"
    },
    {
      "id": "F108",
      "behavior": "The SQL editor becomes write-capable when the session allows, per docs/product-specs/sql-editor.md: the results panel renders affected-row counts for non-SELECT statements; destructive statements open a confirmation dialog showing the classification and the exact statement before resubmitting with the confirmed flag (matching F107's server contract); UPDATE/DELETE without WHERE gets a persistent warning; query history records each statement's classification; and read-only sessions keep today's editor exactly, with a write attempt showing the friendly readOnlyReason instead of a raw rejection. Depends on F097, F107.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/sql-editor.md"
    },
    {
      "id": "F127",
      "behavior": "Column-level SQL autocomplete: the editor's completion (packages/ui/src/query/sql-completion.ts, currently keywords + table names only from F013) gains column suggestions - after a table alias or qualified name (t., \"Table\"., schema.table.) suggest that table's columns; in general expression positions suggest columns of tables referenced in the current statement, using the already-fetched /api/tables metadata (no new server surface); properly quotes identifiers that need it per the connected engine. Read-only-safe, benefits every access level; a professional-IDE table-stakes gap found in the second-pass review. Unit tests cover alias resolution, quoting, and mixed-case identifiers. Depends on nothing in this plan (schedulable any time); placed in Phase C alongside the editor work.",
      "verification": "pnpm --filter @qyre/ui test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/sql-editor.md"
    },
    {
      "id": "F126",
      "behavior": "Query cancellation and long-operation handling: a running SQL-editor statement (and the Rows-table fetch spinner) gains a working Cancel control. Server keeps a registry of in-flight operations per session; adapters implement engine-appropriate cancellation - Postgres: capture pg_backend_pid() per checkout and issue pg_cancel_backend() from a second connection; MySQL: KILL QUERY <processlist id>; MongoDB: cursor maxTimeMS already bounds reads, add best-effort killOp where permitted; SQLite: better-sqlite3 is synchronous and blocks the whole event loop, so this slice must decide (exec plan 0006 open decision #2, spike first) between moving SQLite statement execution to a worker thread (terminable, keeps /api/health responsive during a long scan) or documenting SQLite as non-cancellable with the existing timeout as the only bound. Cancelled operations report a distinct 'cancelled' outcome (not a generic error) in the UI and event log. Conformance: a deliberately slow query per engine is cancelled and the connection remains usable afterwards. Depends on F107.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F109",
      "behavior": "Product-spec pass for schema editing before implementation: create docs/product-specs/schema-editing.md covering the ddl adapter namespace (table lifecycle: create/rename/truncate/drop; column ops: add/rename/drop/alter type-nullability-default; index ops: create/drop), the per-engine DDL matrix - Postgres/MySQL native ALTER; SQLite native ADD/RENAME/DROP COLUMN (3.25/3.35+) where possible and the documented rebuild pattern (PRAGMA foreign_keys=OFF, create-new/copy/swap in one transaction, PRAGMA foreign_key_check after) for retype/constraint changes; MongoDB mapped to collections + indexes with column ops not-applicable - plus each engine's column type catalog exposure for the table designer (open decision in exec plan 0006: static list vs introspected), kind-gating rules (F124: DDL applies to kind 'table'/'collection' only; views/matviews are out of managed scope this plan), typed-confirmation rules for destructive DDL (type the table name to drop/truncate, per docs/SECURITY.md), and the API shapes. Depends on F090, F124.",
      "verification": "pnpm check:state",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md"
    },
    {
      "id": "F110",
      "behavior": "Table lifecycle per docs/product-specs/schema-editing.md: adapters gain the ddl namespace with createTable (columns with types from the engine's exposed type catalog, PK incl. composite, nullability, defaults), renameTable, truncateTable, and dropTable - MongoDB maps to create/rename/drop collection with truncate as deleteMany({}). All operations are kind-gated (F124): targeting a view/matview is rejected up front with a clear error, never sent to the engine as the wrong statement. Server routes gate on the ddl capability + F096 central guard, require the spec's typed-confirmation token for drop/truncate server-side, and audit every operation. Conformance: create-inspect-drop roundtrip on all four engines, restricted-user rejection, unconfirmed-drop rejection, view-target rejection. Depends on F096, F109.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md"
    },
    {
      "id": "F111",
      "behavior": "Column operations per docs/product-specs/schema-editing.md: addColumn/renameColumn/dropColumn/alterColumn (type, nullability, default) - native ALTER on Postgres/MySQL; SQLite uses native ADD/RENAME/DROP COLUMN where the operation and version allow and the spec's rebuild pattern (PRAGMA foreign_keys=OFF, create-new/copy/swap in one transaction, PRAGMA foreign_key_check) for retype/constraint changes, chunking the copy so a large table doesn't block the event loop unboundedly; not-applicable on MongoDB (capability reported false, UI hides the surface). Server routes with typed confirmation for dropColumn, audit events, and conformance including a SQLite rebuild data-preservation case (data, indexes, FKs survive) and restricted-user rejection. Depends on F110.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md"
    },
    {
      "id": "F112",
      "behavior": "Index management per docs/product-specs/schema-editing.md: createIndex (columns, unique flag) and dropIndex in the ddl namespace across all four engines (MongoDB native createIndex/dropIndex), building on the existing IndexMetadata listing from getTable; server routes gated + audited; conformance: create-list-drop roundtrip on every engine, unique-index constraint behavior, restricted-user rejection. Depends on F110.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md"
    },
    {
      "id": "F113",
      "behavior": "Permission-gated New-table flow per docs/product-specs/schema-editing.md: a table designer in the sidebar/Schema surface for name + columns (type picked from the engine's type catalog exposed by F110, nullability, default, PK incl. composite), a preview of the exact DDL/ops before creation, per-engine validation, and engine errors mapped to clear messages; the MongoDB variant degrades to a new-collection dialog (name only). Hidden without ddl capability. Depends on F110.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md"
    },
    {
      "id": "F114",
      "behavior": "Each table gains a Structure view alongside Rows per docs/product-specs/schema-editing.md: columns listed with add/rename/drop/alter controls (capability-gated; hidden where the adapter reports not-applicable, e.g. MongoDB columns), index management (F112), and rename/truncate/drop-table actions behind the spec's typed confirmations - every action previews its DDL/ops first and surfaces the audit outcome. Views/matviews (F124) get the Structure information read-only with zero mutation affordances and their kind stated; read-only sessions likewise see structure info only (a strict improvement to today's read-only experience). E2E covers a column-add + index-create + drop-table roundtrip on one SQL engine, and the F097 read-only guard stays green. Depends on F111, F112, F113.",
      "verification": "pnpm --filter @qyre/ui test && pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/schema-editing.md"
    },
    {
      "id": "F115",
      "behavior": "Database/schema lifecycle where the engine supports it: adapters gain the admin namespace with listDatabases (Postgres/MySQL/MongoDB; SQLite not-applicable - one file is one database and must report that cleanly), createDatabase/dropDatabase, and Postgres schema create/drop; capabilities report supportsDatabaseManagement from the real F092-F095 introspection. Server routes gated + audited; drop requires the typed-confirmation token server-side. Conformance: list/create/drop roundtrip on the three applicable engines, restricted-user rejection, SQLite clean not-applicable. Needs a short spec section added to docs/product-specs/permissions-and-capabilities.md or schema-editing.md (decide during implementation, following F109's patterns). Depends on F096, F110.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F116",
      "behavior": "Database-level management UI: the connection/database switcher (F064/F085 flow) lists databases on the current server with switch-in-place, permission-gated create-database (name plus minimal engine-appropriate options), and drop-database behind typed confirmation; Postgres schema create/drop appears in the sidebar. Read-only or ungranted sessions see the list only, with the affordances hidden and the reason available. Depends on F115.",
      "verification": "pnpm --filter @qyre/web test && pnpm test:e2e",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F117",
      "behavior": "Permission-gated CSV import into an existing table: a server endpoint streams and parses the upload (with an explicit request-size/row-count cap and multipart limits so a huge or malicious file cannot exhaust memory or disk - the F066 streaming-export philosophy in reverse), validates and coerces values per column type against introspected metadata, and inserts in bounded batches through the mutations namespace (transactional per batch, per-row error report with line numbers); the UI wizard maps CSV columns to table columns with a dry-run preview of the first rows and a clear success/failure summary. Kind-gated (tables/collections only) and permission-gated. Needs a short product-spec pass (docs/product-specs/csv-import.md) before implementation, following the F063-F066 pattern. Depends on F099, F102.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/web test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/csv-import.md"
    },
    {
      "id": "F118",
      "behavior": "Row export grows JSON (streamed array of objects) and SQL-INSERT formats (SQL engines only, identifiers/literals safely escaped per engine; MongoDB exports Extended JSON, reported via capabilities not engine-name checks) alongside the existing CSV path, honoring current filters/sort/selection exactly as F066/F083 do. The export implementation also fixes the current O(n^2) pattern (the CSV route re-runs the query per MAX_PAGE_SIZE page via OFFSET pagination - packages/server/src/routes/tables.ts): switch to a single-pass stream per engine (pg cursor/query stream, mysql2 .stream(), better-sqlite3 .iterate(), MongoDB cursor). Read-only-safe, so it improves every access level; update docs/product-specs/server-side-sort-export.md. No dependency on write slices - schedulable any time after F091.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/ui test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/server-side-sort-export.md"
    },
    {
      "id": "F128",
      "behavior": "EXPLAIN viewer for the SQL editor (read-only-safe, a standard IDE capability): an Explain action runs the engine-appropriate plan statement for the current query - Postgres EXPLAIN (optionally ANALYZE behind an explicit toggle with a warning that ANALYZE executes the statement, and never for non-SELECT classifications), MySQL EXPLAIN/EXPLAIN FORMAT=TREE, SQLite EXPLAIN QUERY PLAN; MongoDB not-applicable (no SQL surface) - and renders the plan in a readable dedicated panel (text/tree; no graphical layout this slice). assertReadOnly already allows the EXPLAIN keyword, so read-only sessions get this too. Unit tests for the per-engine statement construction; e2e asserts a plan renders for a fixture query on one engine. Depends on F106 (classification decides ANALYZE eligibility); read-only parts schedulable earlier if picked up independently.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/ui test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": "docs/product-specs/sql-editor.md"
    },
    {
      "id": "F119",
      "behavior": "A read-only Roles & grants viewer so users understand why affordances are hidden: Postgres shows pg_roles plus a grants summary for the current user, MySQL shows SHOW GRANTS/information_schema privileges (including granted roles), MongoDB shows rolesInfo/usersInfo where permitted, SQLite shows file-permission facts instead; strictly a viewer - no role management mutations anywhere in plan 0006. Surfaces through the admin namespace with a capability flag; degrades cleanly when the connected user may not read role catalogs. Depends on F092-F095.",
      "verification": "pnpm --filter @qyre/server test && pnpm --filter @qyre/web test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F120",
      "behavior": "Permission-denied hardening across every mutating path: engine permission errors map to structured, friendly responses (which operation, which object, the likely missing grant) instead of raw engine text; a denial triggers a capabilities/permissions refetch so UI affordances converge with reality when grants change mid-session (the advisory-introspection principle in exec plan 0006); the event log records each denial. Conformance covers a denied insert/update/ddl per engine using the restricted fixtures with grants revoked mid-session where the engine allows. Depends on the write surfaces it protects - schedule after F117.",
      "verification": "pnpm --filter @qyre/server test && node scripts/with-local-env.mjs pnpm --filter @qyre/testing-conformance test",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    },
    {
      "id": "F121",
      "behavior": "Plan 0006's exit gate: a Playwright role-matrix suite runs per engine - a read-only session (restricted fixture user and --read-only flag) shows zero write affordances, keeps every pre-plan read-only workflow green, and receives clean structured rejections from every mutating API route both with a valid session token and (401) without one (F122); a writable session edits and commits a row (SQL grid on one engine, MongoDB document editor), runs a write statement through the destructive-confirmation flow, and creates + drops a table (where the engine supports it) end-to-end. Docs consolidation lands with it: README feature matrix, ARCHITECTURE.md's adding-an-engine recipe gains the capabilities/mutations/ddl/admin steps, and docs/SECURITY.md's write rules are updated from aspirational to implemented contract. Move plan 0006 to exec-plans/completed with evidence. Depends on all prior plan slices.",
      "verification": "pnpm test:e2e:full && pnpm check:state",
      "state": "not_started",
      "commitHash": null,
      "evidence": null,
      "blockedReason": null,
      "spec": null
    }
  ]
}
