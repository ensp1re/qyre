# QUALITY_SCORE.md

Tracks whether the repository is getting stronger or weaker over time. Update as part of normal
work, not as a separate cleanup day.

## Grading scale

- `A`: verified, legible, stable, boundaries enforced
- `B`: working with minor gaps
- `C`: partially working, notable confusion or instability
- `D`: broken, unsafe, or structurally unclear
- `-`: not started

## Packages

| Package                 | Grade | Verification                                             | Agent legibility             | Test stability                                   | Key gaps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Last updated |
| ----------------------- | ----- | -------------------------------------------------------- | ---------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| `@qyre/core`            | B     | `pnpm --filter @qyre/core test` (17/17)                  | good, folder-organized       | stable                                           | `validation/` only covers query+rows so far                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-07-02   |
| `@qyre/driver-contract` | B     | `pnpm --filter @qyre/driver-contract test` (21/21)       | good                         | stable                                           | now backs two engines (F008) - `assertReadOnly` moved here from `@qyre/postgres` so both share one implementation instead of duplicating it                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-07-02   |
| `@qyre/postgres`        | B     | `pnpm --filter @qyre/postgres test` (11/11)              | good                         | stable, integration + security-regression tested | audit found a real exploitable bug (writable CTE bypassed read-only check) and fixed it with a Postgres `READ ONLY` transaction backstop, not just the string check - see F006's audit. F007's audit found a second real bug: an unhandled `pool.on("error")` crashed the whole process when the database connection dropped, defeating `/api/health`'s purpose - fixed and regression-tested. Heuristic string check remains defense-in-depth, not exhaustive. `assertReadOnly` itself moved to `@qyre/driver-contract` (F008); this package now re-exports it.                  | 2026-07-02   |
| `@qyre/sqlite`          | B     | `pnpm --filter @qyre/sqlite test` (12/12)                | good                         | stable, integration-tested against a real file   | new (F008). No CI service dependency at all - SQLite is just a local file, unlike Postgres's container requirement. Read-only enforced by opening the whole connection `readonly: true` (SQLite has no writable-CTE/stored-procedure bypass class for a string-scan to miss, so this is both necessary and sufficient, verified live). Playwright e2e coverage not yet done - split out as F011.                                                                                                                                                                                  | 2026-07-02   |
| `@qyre/server`          | B     | `pnpm --filter @qyre/server test` (8/8)                  | good                         | stable                                           | F007 audited: `/api/health` behavior itself was correct, but the real bug (process crash on DB disconnect) lived in `@qyre/postgres`'s pool, not here. F008 fixed a second, smaller health-endpoint bug: SQLite targets were redacted as `<unparseable connection string>` since redaction assumed every target is a URL - now skips redaction for file-path targets.                                                                                                                                                                                                             | 2026-07-02   |
| `qyre` (cli)            | B     | `pnpm --filter qyre test` (7/7)                          | good                         | stable                                           | web-dist path is monorepo-relative (tech debt, tracked as F010). Now resolves either engine via `resolveAdapter([postgresAdapterFactory, sqliteAdapterFactory], target)` (F008).                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-07-02   |
| `@qyre/ui`              | B     | builds/typechecks; no dedicated tests yet                | good, one component per file | none yet                                         | zero component-level test coverage (only exercised via e2e smoke/full journeys); 6 components now (status-badge, panel, schema-tree, table-detail, rows-table, query-runner). Added `cn()` (clsx+tailwind-merge, DF-01) as the shared helper every future Tailwind/shadcn component here will need - not yet consumed by any component, wired ahead of DF-02's shadcn primitives.                                                                                                                                                                                                 | 2026-07-02   |
| `@qyre/web`             | B     | smoke + full E2E (`pnpm test:e2e`, `pnpm test:e2e:full`) | good, api/hooks split        | stable                                           | F001-F006 all have UI now (connection, nav tree, table metadata, rows, query runner); no component-level tests, only E2E. Engine-agnostic already (F008 exercised it live against SQLite over the same HTTP contract with zero frontend changes needed) - Playwright coverage of that is F011. DF-01: Tailwind was scaffolded from the start but genuinely unused (zero component used a class) - now has real tokens (`docs/references/design-system.md`) wired into `tailwind.config.js`/`index.css`; components themselves still use inline styles until DF-02+ migrates them. | 2026-07-02   |

## Architectural layers

| Layer      | Grade | Boundary enforcement                              | Agent legibility | Key gaps                                                                                                                                                                         | Last updated |
| ---------- | ----- | ------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Core types | B     | docs + folder convention (`CODE_ORGANIZATION.md`) | good             | none major                                                                                                                                                                       | 2026-07-01   |
| Drivers    | B     | docs + `packages/drivers/` grouping               | good             | two engines now (F008) - the split held: `assertReadOnly`, pagination, and the whole `DatabaseAdapter` contract were reused unchanged, only SQL/introspection is engine-specific | 2026-07-02   |
| Server     | B     | docs + `STRUCTURE.md` growth path                 | good             | none major                                                                                                                                                                       | 2026-07-01   |
| CLI        | B     | docs only                                         | good             | web-dist path packaging (tech debt)                                                                                                                                              | 2026-07-01   |
| UI         | B     | docs + folder convention + `STRUCTURE.md`         | good             | no component tests; mid-migration from inline styles to Tailwind/shadcn (DF-01 wired the tokens, DF-02+ migrates components) - see `docs/product-specs/dashboard-ui.md`          | 2026-07-02   |

## Benchmark snapshots

| Date       | Variant                                   | Completion rate | Retries | Defects before review | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ----------------------------------------- | --------------- | ------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-30 | skeleton                                  | n/a             | n/a     | n/a                   | structure + checks only, no product code                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-01 | F001/F003 audit + architecture reorg      | n/a             | n/a     | 4                     | audits of "passing" package tests found real gaps each time (`QYRE_PORT` ignored, no static serving, indexes/rowCount unimplemented, `pg` array-type bug); architecture reorganized into types/components/drivers folders                                                                                                                                                                                                                                                                                                   |
| 2026-07-01 | F002/F004/F005 (connect-and-inspect done) | n/a             | n/a     | 1                     | E2E infra itself was broken (Playwright's `webServer` had no backend at all - `vite preview` alone), so the golden journey could never have passed regardless of frontend completeness; fixed with a real server (`e2e/server.ts`), then finished the UI                                                                                                                                                                                                                                                                    |
| 2026-07-01 | F006 audit (query runner)                 | n/a             | n/a     | 2                     | one **critical, exploitable** defect: writable CTEs bypassed the read-only check and executed real deletes, confirmed against a live table before fixing; fixed with a Postgres `READ ONLY` transaction as the authoritative backstop, not just a better regex                                                                                                                                                                                                                                                              |
| 2026-07-01 | F007 audit (health/diagnostics endpoint)  | n/a             | n/a     | 1                     | one **critical** defect: `/api/health`'s job is to report database connectivity, but the whole Node process crashed instead when the DB connection dropped (unhandled `pool.on("error")`), confirmed live against a real container before fixing                                                                                                                                                                                                                                                                            |
| 2026-07-02 | F008 (SQLite engine)                      | n/a             | n/a     | 1                     | one real defect caught by live manual verification (not package tests, which all passed): `/api/health` redacted a SQLite file path as `<unparseable connection string>` because redaction assumed every target is a URL. Also: `@qyre/core`'s `parseConnectionTarget` turned out to be Postgres-only in a way that would have hard-blocked any second engine before it ever reached `AdapterFactory.supports()` - not caught by tests since none existed for a non-Postgres target; generalized and covered with new tests |
| 2026-07-02 | DF-01 (dashboard UI redesign, foundation) | n/a             | n/a     | 0                     | no defects - a docs/tokens/config slice with no component behavior change; verified zero regression risk directly (Tailwind was scaffolded but unused by any component, confirmed via grep before wiring real tokens) rather than assuming it. New `DF-##` feature series introduced alongside `F###` for frontend/design-driven work.                                                                                                                                                                                      |

## Simplification log

| Date | Component removed | Outcome | Decision |
| ---- | ----------------- | ------- | -------- |
| -    | -                 | -       | -        |
