# Tech Debt Tracker

Debt that is real, acknowledged, and intentionally deferred.

| Date       | Area                 | Debt                                                                                                                                                                                                                                 | Why deferred                                                                                                                  | Risk                                                                                                                                                                                 | Next trigger                                                                                                |
| ---------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| 2026-06-30 | build                | Packages depend on built `dist` of workspace deps at runtime; typecheck uses path mapping to source.                                                                                                                                 | Simple and reliable for a skeleton.                                                                                           | Possible drift between src and dist types if build order breaks.                                                                                                                     | When CI build ordering causes a failure, or when adding many packages.                                      |
| 2026-07-01 | cli                  | `packages/cli` locates `apps/web/dist` via a path relative to its own file (`../../../apps/web/dist`), which only resolves correctly inside this monorepo checkout.                                                                  | Unblocks real local usage and E2E now; a published-package layout for static assets is a separate, bigger decision.           | Once `humb` is published to npm, this relative path will not find the web build inside the installed package; the server will silently skip serving the UI (falls back to API-only). | Before publishing the `humb` package to npm - now tracked as F010 (`docs/FEATURES.json`).                   |
| 2026-07-04 | ui                   | Empty-state copy in `apps/web/src/App.tsx` (~line 146) still says "Postgres or SQLite" - MySQL/MongoDB are fully supported.                                                                                                          | Cosmetic, no functional impact; queued behind the F020-F033 defect batch from the same review pass.                           | Minor credibility ding on the first screen a user sees.                                                                                                                              | Next UI copy pass, or when a fifth engine lands.                                                            |
| 2026-07-04 | ui                   | CSV export/copy (`packages/ui/src/components/rows-table.tsx` `toCsv`) doesn't escape values starting with `=`/`+`/`-`/`@`, so it's vulnerable to spreadsheet formula injection when opened in Excel/Sheets.                          | Data is the user's own local DB opened in their own spreadsheet; low severity, cheap fix queued behind higher-priority items. | An untrusted DB value could execute a formula in the analyst's spreadsheet app on export.                                                                                            | Before CSV export is promoted/advertised as a supported workflow, or first user report.                     |
| 2026-07-04 | mongodb driver       | `normalizeBsonValue` (`packages/drivers/mongodb`) treats BSON `Timestamp` as a `Long` (it subclasses `Long`), losing `{t, i}` semantics; `Code`/`BSONRegExp`/`MinKey`/`MaxKey`/`BSONSymbol` fall through to a generic object branch. | Edge-case BSON types, rare in typical collections; MongoDB support (F015) is basic read-only browsing by design.              | Incorrect display of replication timestamps or rare BSON-typed fields.                                                                                                               | First user report of a Timestamp/rare-BSON-type field rendering wrong.                                      |
| 2026-07-04 | docs                 | `docs/SECURITY.md` references a `.env.example` that doesn't exist at the repo root; `HUMB_TEST_*` integration-test env vars are only discoverable via CI YAML.                                                                       | Current contributors are coding agents that read CI config directly; no human-onboarding friction observed yet.               | Slower onboarding for a new human/external contributor.                                                                                                                              | First external/human contributor, or when the `docker-compose.yml` debt row below is picked up.             |
| 2026-07-04 | drivers              | Postgres/SQLite/MySQL each re-export `assertReadOnly`/`ReadOnlyViolationError` from `@humbdb/driver-contract` verbatim instead of consumers importing from the contract package directly.                                            | Harmless duplication, no functional cost.                                                                                     | None currently; minor maintenance noise if the contract's exports change.                                                                                                            | Next time a `driver-contract` export changes and all three re-exports need touching.                        |
| 2026-07-04 | ui/a11y              | `cell-value-drawer.tsx` and `query-history-drawer.tsx` don't trap Tab focus or restore focus to the trigger control on close.                                                                                                        | Bundled with the larger keyboard-accessibility push tracked as F031 (schema tree).                                            | Keyboard users can tab into obscured background content while a drawer is open.                                                                                                      | When F031 (schema tree keyboard nav) is picked up - do both accessibility passes together.                  |
| 2026-07-04 | ui                   | `apps/web/src/App.tsx`'s `canGoNext` enables Next whenever `rows.length === pageSize`, so an exact-page-size boundary can land on an empty next page.                                                                                | Cosmetic pagination edge case, rare in practice.                                                                              | Minor UX confusion at exact page boundaries.                                                                                                                                         | When row count/pagination is reworked (see the server-side sort/export row below).                          |
| 2026-07-04 | server               | Static assets are served via `@fastify/static` defaults - no compression, no cache-control for hashed bundle assets.                                                                                                                 | Negligible on a localhost-bound tool.                                                                                         | Marginally slower first paint on large bundles.                                                                                                                                      | If bundle size grows enough to matter, or compression is added for another reason.                          |
| 2026-07-04 | consistency          | Page size is hard-coded to 25 in `apps/web/src/hooks/use-rows.ts` vs. the server's default (50) and clamp (200) in `packages/core`/`packages/drivers/contract` - three divergent numbers, no shared constant.                        | Works today; no observed bug from the divergence.                                                                             | A change to one layer silently diverges from the others.                                                                                                                             | Next time page-size behavior is touched, or when the "centralize tuning constants" row below is picked up.  |
| 2026-07-04 | ui                   | Schema tree search (`packages/ui/src/components/schema-tree.tsx`) requires 2+ characters with no hint; a single character silently shows the unfiltered tree.                                                                        | Minor UX papercut.                                                                                                            | "Is search broken?" confusion, especially on databases with short table names.                                                                                                       | Next schema-tree UX pass.                                                                                   |
| 2026-07-04 | docs                 | Engine lists in the root README "Status" section and per-package READMEs are manually maintained prose (already caught one drift - the "Postgres or SQLite" row above).                                                              | No mechanical enforcement yet (`scripts/check-readme.mjs` doesn't check engine lists).                                        | Docs/UI copy silently falls out of sync as engines are added.                                                                                                                        | Next new engine, or when `check-readme.mjs`/`check-state` is extended to assert the engine list.            |
| 2026-07-04 | a11y                 | Connection status is conveyed by dot color alone in some layouts (`schema-tree.tsx`'s `STATUS_DOT_COLOR`) with no guaranteed adjacent text.                                                                                          | Bundled with the broader accessibility pass tracked as F031.                                                                  | Color-blind users can't distinguish connection state in a collapsed sidebar.                                                                                                         | When F031 (schema tree keyboard nav) accessibility work is picked up.                                       |
| 2026-07-04 | reliability          | `retry: false` on every query hook (`use-health`, `use-overview`, `use-table`, `use-rows`, `use-files`, `use-console`) means a single transient blip immediately surfaces as an error panel.                                         | Reasonable current default (fail fast, explicit Retry); no report yet of spurious errors.                                     | Momentary connection hiccups produce user-visible error states that need manual dismissal.                                                                                           | First real report of spurious error panels on an otherwise-healthy connection.                              |
| 2026-07-04 | architecture         | Postgres and MySQL adapters duplicate `getOverview`/`getTable`/`runReadOnlyQuery`'s transaction ceremony almost verbatim, with no shared `SqlAdapterBase`.                                                                           | Two engines isn't yet enough duplication to justify the abstraction; premature extraction risk.                               | Every new SQL engine re-implements the same boilerplate, and fixes (e.g. F032's statement timeout) must land N times and can drift.                                                  | Before/while adding a third `information_schema`-style SQL engine.                                          |
| 2026-07-04 | architecture         | `DatabaseAdapter` assumes a relational shape; MongoDB fakes fields (`dataType: "any"`, empty indexes) and the UI branches on `engine === "mongodb"` string checks instead of declared capabilities.                                  | Works for the current 4 engines; capability modeling is a contract-wide redesign, not a quick slice.                          | The next non-relational engine multiplies engine-name conditionals across the UI and server.                                                                                         | Before adding a 5th engine, especially a non-relational one.                                                |
| 2026-07-04 | architecture         | The adapter/factory registry, `scripts/publish.mjs`'s `PUBLISH_ORDER`, and `connection-target.ts`'s protocol detection are three separately hand-maintained lists that must stay in lockstep.                                        | Manageable at 4 engines.                                                                                                      | Adding an engine can wire the driver but forget detection or publish order.                                                                                                          | Before adding a 5th engine, or the first time these lists are caught drifting.                              |
| 2026-07-04 | frontend             | `apps/web/src/App.tsx` is a ~290-line component with one deeply nested tab-routing ternary mixing routing, loading/error/empty states, and data wiring.                                                                              | Works correctly; splitting it is a pure refactor with no behavior change, lower priority than the F020-F033 defects.          | Hardest file in the app to read/test; every new tab deepens the nesting.                                                                                                             | Next time a new tab is added.                                                                               |
| 2026-07-04 | maintainability      | Tuning constants (page sizes, `MAX_EVENTS`, `FIELD_SAMPLE_SIZE`, history `MAX_ENTRIES`) are defined independently per package with no shared surface.                                                                                | Overlaps with the page-size-divergence row above; not urgent standalone.                                                      | Related limits can't be reasoned about or tuned in one place.                                                                                                                        | Same trigger as the page-size-divergence row, or next time two of these constants need to change together.  |
| 2026-07-04 | reliability          | `/api/health` reports only `connected`/`disconnected`/`unconfigured` plus version - no ping latency or last error message.                                                                                                           | Current signal is enough to unblock F029 (health auto-refresh); richer diagnostics is a separate enhancement.                 | "Disconnected" and "slow" look identical; least actionable answer from a diagnostics endpoint.                                                                                       | After F029 lands, or first user report that health status isn't informative enough.                         |
| 2026-07-04 | cli                  | The `SIGINT`/`SIGTERM` handler has no shutdown timeout, no re-entrancy guard, and doesn't exit non-zero on teardown failure.                                                                                                         | Hasn't caused an observed hang.                                                                                               | A wedged DB connection could block Ctrl-C forever.                                                                                                                                   | First report of the CLI not exiting on Ctrl-C.                                                              |
| 2026-07-04 | performance/security | `runReadOnlyQuery` appends no `LIMIT`; `SELECT * FROM huge_table` fetches/serializes/renders every row with no virtualization.                                                                                                       | Overlaps with F032 (statement timeouts); the full fix (result cap + virtualization) needs its own scoped pass.                | A single query can OOM the server or freeze the browser tab.                                                                                                                         | After F032 lands, or first report of the SQL editor freezing on a large result set.                         |
| 2026-07-04 | performance          | `RowsTable` and the query-result table render every row/cell as DOM nodes with no virtualization.                                                                                                                                    | Current page caps (<=200 rows) keep it tolerable today; compounds with the unbounded-results row above if that lands first.   | Wide tables at max page size already mount thousands of cells.                                                                                                                       | When the unbounded-SQL-results row above is picked up - do both together.                                   |
| 2026-07-04 | performance          | Postgres `getTable` issues 5 separate round-trips per table (columns, PK, FK, indexes, row-count) instead of a combined catalog query.                                                                                               | Distinct from F027 (that's about table _count_; this is per-table query count) - lower priority standalone.                   | Multiplies with F027; slower table-detail loads on large schemas.                                                                                                                    | When F027 (Schema tab fan-out) is picked up - do both together.                                             |
| 2026-07-04 | devx                 | No committed `docker-compose.yml` for the local Postgres+MySQL+Mongo test stack; env vars are only discoverable via CI YAML.                                                                                                         | Current contributors are coding agents reading CI config directly.                                                            | Slow onboarding for a new human contributor.                                                                                                                                         | First external/human contributor, or alongside the `.env.example` row above.                                |
| 2026-07-04 | devx/docs            | No human-facing `CONTRIBUTING.md`; onboarding docs (`AGENTS.md`, `.claude`/`.cursor` skills) are written for coding agents.                                                                                                          | Same as the `docker-compose.yml` row - no human contributors yet.                                                             | Barrier to outside/human contribution.                                                                                                                                               | First external contribution request, or alongside the `docker-compose.yml` row.                             |
| 2026-07-04 | product              | No zero-setup way to try Humb (`--demo` mode with a bundled sample DB); every path needs a real database.                                                                                                                            | Larger product feature; needs its own product-spec pass before implementation.                                                | Adoption friction for first-time trial.                                                                                                                                              | When adoption/growth becomes a priority - scope it via a product spec first.                                |
| 2026-07-04 | testing              | No shared parametrized adapter-conformance test suite (pagination clamping, empty-collection handling, bigint/date fidelity, read-only rejection, error shapes) across Postgres/MySQL/SQLite/Mongo.                                  | Per-engine integration tests cover this ad hoc today; overlaps with the `SqlAdapterBase` row above.                           | An engine can silently diverge in behavior - the class of bug F019 already fixed once.                                                                                               | Before/alongside the `SqlAdapterBase` row, or the next cross-engine consistency bug.                        |
| 2026-07-04 | testing              | `@humbdb/ui` has zero component-level test coverage per `docs/QUALITY_SCORE.md`; only exercised via E2E.                                                                                                                             | E2E currently covers the critical paths.                                                                                      | Slow feedback loop on UI-only logic (cell rendering, tree search, CSV escaping, sort).                                                                                               | Alongside F031 (schema tree accessibility), or the next UI-logic bug E2E is slow to catch.                  |
| 2026-07-04 | testing              | Integration tests skip silently when `HUMB_TEST_*` env vars are unset (green locally without a DB); E2E has no accessibility (axe) assertions.                                                                                       | CI always sets the env vars today, so the silent-skip risk hasn't materialized.                                               | A CI config change could make "green" mean "skipped" without anyone noticing; no automated a11y regression coverage.                                                                 | Any CI service config change, or after F031 lands (add axe checks then).                                    |
| 2026-07-04 | docs                 | No user-facing reference for per-engine connection-string formats, troubleshooting, or a diagram of the read-only/local-first security model.                                                                                        | Current docs (`AGENTS.md` routing) are agent/contributor-facing; end-user docs are a separate audience.                       | "How do I connect to X" support burden as the user base grows.                                                                                                                       | Alongside the `CONTRIBUTING.md` row, or first recurring "how do I connect" issue.                           |
| 2026-07-04 | product              | FK columns aren't clickable even though `getTable` already knows which columns are FKs.                                                                                                                                              | Needs its own UX design (target table, filter, back-navigation).                                                              | None; missed opportunity, not a defect.                                                                                                                                              | Next Tables/Schema tab UX pass - the metadata already exists so cost is mostly UI wiring.                   |
| 2026-07-04 | product              | The Files tab previews `.sql` files but can't run them into the SQL editor.                                                                                                                                                          | Needs to reuse the read-only gate and editor-mount path; small but not zero scope.                                            | None; missed opportunity.                                                                                                                                                            | Next Files tab pass.                                                                                        |
| 2026-07-04 | product              | No way to switch database connections without restarting the CLI.                                                                                                                                                                    | Larger feature (recent-targets UI, re-detection); needs its own spec.                                                         | None directly; friction for multi-database workflows.                                                                                                                                | Needs a product-spec pass before implementation - not a quick slice.                                        |
| 2026-07-04 | product              | `RowsTable` sort is client-side over the current page only ("sort these 25 rows"); CSV export likewise covers only the current page, not the whole table.                                                                            | Needs server-side `ORDER BY` plumbing and a streamed export path - bigger than a one-session slice.                           | Sort/export can silently mislead users on large tables.                                                                                                                              | Alongside the virtualization/per-table-query-fan-out rows above (both touch `RowsTable`/query performance). |
| 2026-07-04 | product              | Loading states are plain "Loading..." text; the only keyboard shortcut (Cmd/Ctrl+Enter) is undiscoverable beyond one hint.                                                                                                           | Cosmetic polish, no functional issue.                                                                                         | None; minor perceived-performance and discoverability gap.                                                                                                                           | Next design-system pass on loading states, or when a command palette is considered.                         |
