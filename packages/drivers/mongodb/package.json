{
  "name": "@qyre/mongodb",
  "version": "0.1.0",
  "type": "module",
  "description": "MongoDB adapter for Qyre (basic read-only browsing).",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@qyre/core": "workspace:*",
    "@qyre/driver-contract": "workspace:*",
    "mongodb": "^7.4.0"
  },
  "devDependencies": {
    "@qyre/config": "workspace:*",
    "@qyre/testing": "workspace:*"
  }
}
