{
  "name": "@humbdb/server",
  "version": "0.0.3",
  "type": "module",
  "description": "Local HTTP server for Humb (Fastify).",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@fastify/compress": "^9.0.0",
    "@fastify/static": "^9.1.3",
    "@humbdb/core": "workspace:*",
    "@humbdb/driver-contract": "workspace:*",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@humbdb/config": "workspace:*"
  }
}
